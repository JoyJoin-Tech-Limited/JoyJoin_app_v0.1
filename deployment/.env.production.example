# JoyJoin Production Environment (yuejuapp.com)
NODE_ENV=production
PORT=5000

# Database (Production)
DATABASE_URL=postgresql://user:password@prod-db.neon.tech/joyjoin?sslmode=require

# AI Services
DEEPSEEK_API_KEY=sk-xxx

# MiniMax AI (Social Icebreaker hybrid provider)
# Get your API key from https://platform.minimax.io
# When set, generateWarmupTopics / generateXiaoYueComment / generateRecapSummary /
# generateLieDetectiveStatements will use MiniMax instead of DeepSeek.
# If unset, all Social Icebreaker functions fall back to DeepSeek automatically.
MINIMAX_API_KEY=
# Optional: override MiniMax base URL (default: https://api.minimax.io/v1)
# MINIMAX_BASE_URL=https://api.minimax.io/v1
# Optional: override MiniMax model (default: MiniMax-Text-01)
# MINIMAX_MODEL=MiniMax-Text-01
# Optional: Social Icebreaker AI provider mode (hybrid | deepseek | minimax)
# hybrid  = MiniMax for emotion functions, DeepSeek for structural functions (default)
# deepseek = all Social Icebreaker functions use DeepSeek
# minimax  = all Social Icebreaker functions use MiniMax (requires MINIMAX_API_KEY)
SOCIAL_AI_PROVIDER=hybrid

# Authentication & Session
JWT_SECRET=production-jwt-secret-use-strong-random-value
SESSION_SECRET=production-session-secret-use-strong-random-value

# WeChat Credentials
# Used for BOTH flows:
#   ① Mini Program login (小程序): jscode2session  → /api/auth/wechat/login*
#   ② Official Account web login (公众号网页授权): oauth2/authorize → /api/auth/wechat/oauth/*
# JoyJoin's Mini Program and Official Account are bound under the same WeChat Open Platform
# account, so they share a single AppID/Secret and a unified UnionID namespace.
# No separate WECHAT_OA_APPID is needed.
WECHAT_APPID=wx5a038ee6dee12032
WECHAT_SECRET=<your_wechat_app_secret>

# Public-facing base URL of the app.
# /api/* is served from this same origin via the Caddy reverse proxy (path-based, NOT subdomain
# — see deployment/Caddyfile: yuejuapp.com { handle /api/* { ... } }). This single variable
# covers the WeChat OAuth2 redirect_uri AND the post-login redirect target.
# The "网页授权域名" registered in the WeChat OA backend must match this domain (yuejuapp.com).
APP_URL=https://yuejuapp.com

# Cross-subdomain cookie sharing (注意前面的点号)
# 允许 api.yuejuapp.com, admin.yuejuapp.com, www.yuejuapp.com 共享登录状态
COOKIE_DOMAIN=.yuejuapp.com

# CORS (Production domains)
CORS_ORIGINS=https://yuejuapp.com,https://www.yuejuapp.com,https://admin.yuejuapp.com,https://api.yuejuapp.com

# Frontend URLs (通过 Caddy 反向代理访问，无需暴露端口)
VITE_API_URL=https://api.yuejuapp.com
