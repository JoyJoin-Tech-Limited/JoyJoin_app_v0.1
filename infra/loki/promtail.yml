server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Tail JoyJoin container stdout/stderr via Docker socket
  - job_name: joyjoin_docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: name
            values:
              - joyjoin
    relabel_configs:
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: container
      - source_labels: ["__meta_docker_container_log_stream"]
        target_label: logstream
    pipeline_stages:
      # Parse structured JSON log lines emitted by apps/server/src/lib/logger.ts
      - json:
          expressions:
            level: level
            service: service
            request_id: request_id
            message: message
      - labels:
          level:
          service:
      - output:
          source: message
