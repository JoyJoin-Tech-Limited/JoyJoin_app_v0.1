name: JoyJoin Production Pipeline

on:
  push:
    branches: [ main ]

concurrency:
  group: joyjoin-prod-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint-user:
    name: Type Check User Client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Debug workspace + lockfile
        run: |
          set -e
          echo "PWD=$(pwd)"
          echo "WORKSPACE=$GITHUB_WORKSPACE"
          ls -la
          find . -maxdepth 4 \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) -print
          test -f package-lock.json || (echo "❌ package-lock.json NOT FOUND at repo root" && exit 1)

      - name: Install dependencies
        run: npm ci

      - name: Run TSC
        run: npx tsc -p apps/user-client/tsconfig.json --noEmit

  lint-admin:
    name: Type Check Admin Client
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Debug workspace + lockfile
        run: |
          set -e
          echo "PWD=$(pwd)"
          echo "WORKSPACE=$GITHUB_WORKSPACE"
          ls -la
          find . -maxdepth 4 \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) -print
          test -f package-lock.json || (echo "❌ package-lock.json NOT FOUND at repo root" && exit 1)

      - name: Install dependencies
        run: npm ci

      - name: Run TSC
        run: npx tsc -p apps/admin-client/tsconfig.json --noEmit

  lint-server:
    name: Type Check Server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Debug workspace + lockfile
        run: |
          set -e
          echo "PWD=$(pwd)"
          echo "WORKSPACE=$GITHUB_WORKSPACE"
          ls -la
          find . -maxdepth 4 \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) -print
          test -f package-lock.json || (echo "❌ package-lock.json NOT FOUND at repo root" && exit 1)

      - name: Install dependencies
        run: npm ci

      - name: Run TSC
        run: npx tsc -p apps/server/tsconfig.json --noEmit

  ai-test:
    name: AI Simulation Test
    needs: [lint-user, lint-admin, lint-server]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Debug workspace + lockfile
        run: |
          set -e
          echo "PWD=$(pwd)"
          echo "WORKSPACE=$GITHUB_WORKSPACE"
          ls -la
          find . -maxdepth 4 \( -name "package-lock.json" -o -name "npm-shrinkwrap.json" \) -print
          test -f package-lock.json || (echo "❌ package-lock.json NOT FOUND at repo root" && exit 1)

      - name: Install dependencies
        run: npm ci

      - name: Run Simulation
        run: npx tsx apps/server/src/tests/runSimulation.ts 100
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}

  deploy:
    name: Production Deployment
    needs: ai-test
    runs-on: ubuntu-latest
    steps:
      - name: SSH Remote Deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script: |
            set -euo pipefail

            # ====== HARD-CODED DATABASE_URL (as requested) ======
            # 注意：不要 echo 全量，避免写进 action log
            export DATABASE_URL="postgresql://neondb_owner:npg_NmTv6SY3fxXW@ep-square-math-ahiz6fm7-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require"

            soft_cleanup() {
              echo "🧹 Soft cleanup (keep 7 days)..."
              docker builder prune -af --filter "until=168h" || true
              docker system prune -af --filter "until=168h" || true
            }

            hard_cleanup() {
              echo "🔥 Hard cleanup (aggressive, no volumes)..."
              docker builder prune -af || true
              docker system prune -af || true
            }

            disk_guard() {
              local usep
              usep=$(df -P / | awk 'NR==2{gsub("%","",$5); print $5}')
              if [ "${usep}" -ge 85 ]; then
                echo "⚠️ Disk usage is ${usep}%. Cleanup before deploy..."
                soft_cleanup
              fi
            }

            on_fail() {
              echo "❌ Deploy failed. Running hard cleanup to prevent builder cache explosion..."
              hard_cleanup
              df -h /
            }
            trap on_fail ERR

            echo "🧭 Disk before:"
            df -h /

            disk_guard

            # --- 1) Code Sync ---
            echo "📥 Syncing code..."
            cd ~/JoyJoin
            git fetch --all
            git reset --hard origin/main

            # --- 2) Restart containers ---
            echo "🐳 Restarting containers..."
            cd ~/JoyJoin/deployment
            docker compose -f docker-compose.caddy.yml up -d --build --remove-orphans

            # --- 3) DB Sync (IMPORTANT: use the SAME DATABASE_URL as API) ---
            echo "🗄️ Syncing database schema..."
            cd ~/JoyJoin
            # 强制同一条连接串，避免“假 push”
            npx drizzle-kit push --config=./drizzle.config.ts

            # --- 4) Health checks ---
            echo "🏥 Verifying service status..."
            sleep 10

            echo "   Checking internal API..."
            curl -s --retry 5 --retry-delay 3 http://localhost:5000/api/health > /dev/null \
              || (echo "❌ API Failed" && docker logs joyjoin-api --tail 120 && exit 1)

            echo "   Checking Caddy..."
            if [ "$(docker inspect -f '{{.State.Running}}' joyjoin-caddy 2>/dev/null || echo false)" != "true" ]; then
              echo "❌ Caddy is NOT running."
              docker logs joyjoin-caddy --tail 80 || true
              exit 1
            fi

            # --- 5) Post-deploy cleanup (prevents builder cache from filling disk) ---
            soft_cleanup

            echo "🧭 Disk after:"
            df -h /
            echo "✅ Deployment successful!"


            # 如果 deploy 失败：做 hard cleanup（释放空间），并把失败抛出去（让 workflow 红）
            on_fail() {
              echo "❌ Deploy failed. Running hard cleanup to prevent cache explosion..."
              hard_cleanup
              echo "🧭 Server disk status (after hard cleanup):"
              df -h /
            }

            trap on_fail ERR
            run_deploy
