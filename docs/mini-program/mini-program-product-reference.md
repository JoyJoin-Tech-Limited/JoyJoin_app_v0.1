# Mini-Program Product Reference

> **Status:** Active launch-primary mini-program reference for `apps/mini-program`.
> **Last verified:** 2026-08-06.
> **Last updated:** 2026-08-17 (「订座」仪式重构 P0+P1: pool-registration and event-ticket-payment success surfaces converge on a shared `RegistrationSuccessCeremony` + `ReservationTicket`; the paid variant renders a tear-off ticket stub and a CSS scale-in `已留座`/`已确认` seal; the seal landing fires `socialHaptics('socialCelebration')` exactly once. Confirm modal and payment ticket both reuse the same `ReservationTicket` flat variant. Legacy `eventTicketSuccess` ceremony hero, `PoolRegistrationSuccess`, and `TicketSuccessView` removed. See `docs/design/registration-ceremony-spec-20260817.md`.)
> Plus: 2026-08-16 (Tablemate card-deck reskin: matching-status matched carousel and pool-group-detail deck strip now reuse the squad-unboxing `TablemateCard` front-face recipe via `apps/mini-program/src/components/TablemateCard`; member detail surfaced in `TablemateDetailSheet`; shared pair-chemistry helpers moved to `apps/mini-program/src/lib/utils/pairChemistry.ts`; `verify-subpackage-styles.mjs` REQUIREMENTS gained `pages/matching-status` and `pages/pool-group-detail` entries).
> Plus: 2026-08-06 (Lovart Xiaoyue portrait set v1 wired into `PoolRegistrationMascotSection` reduced-motion path).
> Plus: 2026-08-05 (Pool-registration mascot section — contract `pool-reg-mascot-section-20260805`: Steps 1–3 render a dedicated 悦仔 row directly above step content (mascot-left 160rpx animated sprite, bubble-right max 520rpx, radius 24rpx, left tail). The section owns its sprite-state machine: forward step entry plays a one-shot pop + bubble rise (no replay on back nav); first selection per step fires a one-shot `nod` (~900ms manifest) with bubble swap (out 120ms / in 160ms); idle is breathing-only (translateY 0→-4rpx, 2.4s); reduced/degraded motion renders static portraits with content swaps preserved. `XiaoyueCoachCard` lost its mascot-wrap (header keeps eyebrow + archetype tint); the three `hideAvatar` `XiaoyueChatBubble` reactions and `useReactionTimer.ts` were removed (state machine replaces the timer — REL-01). Step-intro bubble copy lives in `flowConfig.ts` (`getMascotStepIntro`, `getStepReactionLine`); reaction lines reuse previously approved copy; 随缘-only reactions reuse the approved flexible feedback line. `verify-subpackage-styles.mjs` REQUIREMENTS gained a `pages/pool-registration` entry (AC-12).)
> Plus: 2026-08-06 (Lovart Xiaoyue portrait set v1 — dedicated reduced-motion portraits: `PoolRegistrationMascotSection`'s reduced/degraded-motion path now renders the commissioned 3-pose portrait set (`lovart-mascot-xiaoyue-{coach,curious,listening}-20260805-v1.webp`, 480×480, ~48–50KB each) via `PORTRAIT_URL_BY_SPRITE_STATE` (`cdnAsset()`, CDN-primary + local bundle) instead of the shared expressions (`coachGuide`/`testCurious`/`testListening`). PNG masters in `assets-source/lovart/xiaoyue-portraits/`; shipped WebP in `src/assets/personality/xiaoyue/`; entries added to `cdn-asset-manifest.json`. Brief: `docs/design/lovart-brief-xiaoyue-portraits-20260805.md` (marked delivered).)
> Plus: 2026-08-18 (WeChat review-posture copy softening: all user-facing 匹配→排桌/合拍, 社交→活动/聚会, 快速交友→轮桌畅聊, 撮合→牵线, 灵魂→一拍即合/搭子 across mini-program + shared copy + server notifications + legal terms; AIGC terms clause made conditional to match the no-AI submission posture. Canonical policy: `docs/runbooks/wechat-review-submission.md`.)
> Plus: 2026-08-05 (Intent selection redesign: the 3-intent cap is removed for pool-registration — `toggleIntentValue` defaults to unlimited (`maxExplicit: Infinity`) and auto-collapses the selection to `flexible` (随缘) when every explicit option is selected; `PoolRegistrationIntentGrid` lost cap-disable logic and the 「最多选择 3 个期待」 toast; profile surfaces (`essential-data`, `edit-profile`) keep their explicit 3-cap. `IntentCard` restructured as icon-led tiles: 144rpx hero icon centered, label + subtitle caption below, `min-height` 176→280rpx; icon sizes enlarged +50% on edit-profile (36→54rpx) and profile-review chips (24→36rpx). `intent-explore.webp` (尝鲜体验) delivered via Lovart and wired — the last intent icon without a branded asset (was rendering raw 🎯 emoji); converted to 144×144 WebP (5.2KB) and re-scaled from source to match sibling visual weight (69%×62% bbox / 26% fill vs sibling band). Brief: `docs/design/lovart-brief-intent-explore-20260805.md`. Copy (provisional): Step 2 intro 「这次最想收获什么？可以多选，选全了就交给悦仔」, empty feedback 「先选几个期待方向，或者直接选随缘。」)
> Plus: 2026-08-05 (Teaser strip Phase 3 + 4: the flow-node entrance was redesigned from a parallel stagger to a sequential left-to-right light-up — nodes sit ghosted (0.35 opacity) and illuminate one-by-one (0.24s each, 0.16s step, ~1.3s total) while a new `pool-teaser__track` draw-line sweeps beneath the icons in sync (transform/opacity only, GPU-safe; reduced-motion/static unchanged). `verify-subpackage-styles.mjs` pool-registration entry extended with `pool-teaser__track` + `pool-teaser-node-light` (6 selectors, PASS). Phase 4 voice-line copy (copy-owner sign-off 2026-08-05, revised same day to value-first framing): 「我背后是自研 AURA 引擎，会帮你找到磁场对路的人」 — names the matching engine by its sentence-derived codename **AURA** ("Attraction Unites, Resonance Aligns.", 2026-08-05; formal internal name remains 磁场引擎/Magnetism Engine per `docs/systems/MAGNETISM_ENGINE.md` §1; 磁力引擎 banned; user-facing mentions use 「AURA 引擎」); reuses the approved one-liner vocabulary 「磁场对路的人」; the original process promise 「会一步步帮你把这场局凑起来」 was dropped as too vague (the flow nodes below already carry the how). This line overrides the 🔴 no-AI/算法/权重 self-explanation rule by owner sign-off. Node-label softening (approved 2026-08-05, revised post-audit): teaser node 2 悦仔组局→**等待同频** (4 chars — the 6-char version truncated at ~98rpx/node, audit B1), 这桌成形→桌友到齐; the Flow 2 overlay keeps 等待同频桌友 (vertical layout has room). Closing beat (2026-08-05 polish): 「接下来交给悦仔，到日子见。」 fades in 1.15s after the sweep (copy-owner sign-off pending). Audit fixes same day: track draw 1.04s→0.8s (lands with node 6), ghosted idle states, `--motion-off` breathing kill, keyed bubble-text swap animation. Wider-copy scan (2026-08-05): fixed a real 🔴 violation in `PoolRegistrationTerminalStates` already-joined state — 「已经在匹配引擎里跑着了」 → 「悦仔正在帮你挑同频的桌友」 (mechanism word removed, value-first); `AI生成` content labels in icebreaker phases confirmed as required transparency disclosure (not violations).)
> Plus: 2026-08-05 (Event-ticket-payment Wave 1 conversion polish — PM+UIUX consolidated strategy): (0) branded loading state replaces bare 「加载中…」 (悦仔 `loadingSystem` + dots); (1) cancel-retention anti-nag — first WeChat Pay cancel shows a soft inline note 「你的选择已保存，随时回来继续」 in the sticky footer (audit fix: was below the fold in the scroll area), second cancel opens a once-per-session sheet 「报名信息已保存」 (去支付 / 稍后再说; copy corrected to 报名信息已保存 — the seat is NOT locked pre-payment; `aria-modal` + mask button per audit); (2) continuity subtitle in the choices section 「悦仔把你的预算、期待和细节都备好了」 (悦仔 voice per audit); (3) `paymentTrust` mascot + warm line 「有悦仔在，这场局会好好办」 at the trust row (secure-pay line retained); (4) quiet factual seat signal 「已有 X 人入座」 (registrationCount-backed, deliberately unstyled — no 满员即止 urgency framing per no-FOMO principle). Abandon link softened to 「稍后再报名」 (the original 「已保存你的选择」 over-promised — storage is cleared on abandon). Analytics: `pay_cancel_retention_shown/tap/dismiss` whitelisted client + server. Rejected per consolidation: refund-promise line (policy-untrue until auto-refund ships), merged savings total (two money types), teaser whisper (timing + spoiler). Flow-animation overlay copy synced same day: node titles 悦仔组局→等待同频桌友 / 这桌成形→桌友到齐, 「正在一步步成形/发生」→「正在成形」.)
> Plus: 2026-08-05 (Auto-refund pipeline — sprint auto-refund-pipeline-20260805, Tier 3: 场次未成行 now auto-refunds. Trigger A: admin pool cancellation → every paid registration refunded (completed event payments via `paymentService.createRefund` reason 「活动取消自动退款」; `MOCK_`-prefixed orders finalized directly without WeChat API; consumed event-pack credits restored via `eventCreditsRepo.reverseRedemptionForRegistration` — grant remainingCredits +1, redemption row deleted, idempotent by unique registrationId). Trigger B: `executePostMatchCommitSideEffects` refunds registrations left `matchStatus=unmatched` after commit (reason 「场次未成行，自动退款」; matched users untouched). In-app notifications per user (types `pool_cancelled_refund` / `unmatched_refund`, money + credit variants); single WeCom ops summary per run (`notifyAutoRefundSummary`, critical channel when failures exist). Kill switch `AUTO_REFUND_ENABLED` (default true, now registered in FLAG_ENV_MAP — admin toggle + env fallback work). QA review (2026-08-05) fixed 3 P0s: atomic refund claim (`paymentsRepo.claimPaymentForRefund` completed→refund_pending BEFORE the WeChat call + `releasePaymentRefundClaim` revert — concurrent double-refund impossible), delete-first credit reversal (concurrent double-restore impossible), notify-failure isolation (`notifyUserSafely` never classifies a successful refund as failed). **Smoke test (2026-08-05): ALL PASS** — `apps/server/src/scripts/smoke-auto-refund.ts` (run via `npm run smoke:auto-refund -w @joyjoin/server`), end-to-end vs dev DB: Trigger B refunds only the unmatched user, Trigger A refunds the rest + restores the consumed credit (2→3), both re-runs no-op, self-cleaning. Smoke also caught a **pre-existing bug**: `reconcileUserCreditCache` wrote a raw-SQL string into a timestamp column (`value.toISOString is not a function` — would have broken event-pack grants) — fixed with `::timestamp` cast + Date normalization. Payment-page copy 「场次未成行，全额原路退回」 ships separately after deploy (copy-owner sign-off pending; real-WeChat staging smoke still pending — the only leg not exercisable locally).)
> Plus: 2026-08-05 (Slim resume notice — contract slice 2: unpaid resume state renders one line 「选择已留好，确认后继续加入这场局。」 (shortened post-audit from 「你的选择已经帮你留好了…」 — the longer version wrapped to 2 lines at 375w) with a primary check icon + persistence pills; both handoff codes (`NO_ACTIVE_ENTITLEMENT`, `NO_AVAILABLE_EVENT_PACK_CREDITS`) collapse into this same notice and the handoffCode stays in the return context for the payment page only; paid state kicker 「权益已到账」→「已确认」. Zero occurrences of the string 权益 in `pages/pool-registration/` — payment surfaces (`blind-box-payment`, `payment-verification`) intentionally keep it.)
> Plus: 2026-07-26 (Profile V1.7 visual polish: `IdentityStageScene` stage entrance snap-back removed, partner visual anchored to stage bottom-right, `ArchetypeHead` circle icon + archetype-HSL ring + grid emblem on copy card, warm brand-tinted glass cards / purple equipment bar / three semantic stat cards, RPG-style nameplate/XP-sheen animations; regression tests/typecheck/guardrails/package-size green.)
> Plus: 2026-07-21 (我的形象屏幕根本修复：资产管线从孤立衣物图→`atlas-source.png` 角色差分提取（48/48 层 0.0–0.4% 硬贴合质量通过；12 人格 `fullStarter` 完整穿搭正式图）；子包 WXSS 样式分裂 P0 修复（`verify:subpackage-styles` CI 门禁）；`IdentityStageScene` 落日街道纵深舞台 + 角色身上点选槽位 + 换装淡入；stage webp 加入 CDN 清单；构建脚本误删 v2 非生成文件修复。验证：1253 测试 + typecheck + guardrails + 包体 + 子包样式全绿。前置：需 `npm run upload:cdn-assets` 上传新资产。)
> Plus: 2026-07-17 (PhaseHeroCard PR3: all 9 non-warmup phases migrated to `PhaseHeroCard` shared premium frame with per-phase foil accent (micro_challenge, lie_detective, personality_dice, group_mirror, quip_battle, undercover_word, auction, speed_friending, mini_script, recap gold trim); `phaseViews.tsx` now exports hero views exclusively; `phaseAccents.ts` + `_phase-hero-card.scss` added; feature flag `socialIcebreakerPhaseHeroEnabled` removed; session subpackage 40.8→30.3 KiB; dead `useTierReveal` hook removed. Warmup hero card state machine extracted to `warmupViewModels.ts`; `MomentCardView` overhaul with keepsake topic editorial block; `socialIcebreakerYuezaiCopy.ts` centralised 悦仔 permission lines `YUEZAI_PERMISSION_LINES` keyed by `SocialTopicDepthLevel`. Lovart phase art bands (5× 1500×560 WebP, CDN-only) wired to PhaseHeroCard `artUrl`. H5 screenshot infra extended for icebreaker states.)
> Plus: 2026-07-13 (Alang 页面均以 `GET /api/alang/missions/:slug` 的 `myProgress` 纠正陈旧 URL；普通用户跳过内部配置，`routeDestination` 只在 companion 及以后阶段披露；搜索辅助地图只显示用户本人。)
> Plus: 2026-07-07 (FootprintOracleCard two-rail layout hardening: 168rpx right rail carries a compact segmented countdown, group-size hint, price, or a "待公布" placeholder; rail contents are screen-reader accessible via `railAriaLabel` while only the decorative `›` cue is `aria-hidden`; Lovart corner vignette is clamped to the rail width to avoid overlapping body text.)
> Plus: 2026-07-24 (Corner vignette text overlap: All text layers (title, meta, badge) now intentionally flow over the light (opacity 0.5) corner vignette, removing the previous padding-right reservation. Non-rail vignette enlarged 220→280rpx. OracleCard `--has-corner-stat` reserves only 120rpx on the topline for the solid countdown badge; hero/facts/meta/teaser flow full-width. FootprintOracleCard non-rail vignette 220→280rpx; rail mode unchanged (vignette 168rpx inside rail zone). Squad-unboxing event-brief vignette: repositioned to `absolute` top-right, 112→160rpx, date text flows underneath. Readability relies on vignette opacity 0.5 + text color contrast; no scrim/shadow added. SCSS-only change across `discover/index.scss`, `FootprintOracleCard.scss`, `squad-unboxing/index.scss`.)
> Plus: 2026-06-30 (Profile-review invite teaser: added `ProfileReviewInviteCard` with Lovart `invite-teaser.webp` illustration, CDN-first loading with local fallback, disabled/busy states, and predictive `GET /api/shell/discover` prefetch triggered on reveal via `PrefetchEngine`; analytics `profile_review_invite_impression`, `profile_review_invite_tap`, `profile_review_discover_prefetch_hit`. Center-hub empty-state redesign: hero card with Xiaoyue coach mascot + 4-step "活动流程" flow card; connections empty-state redesign: hero card with Xiaoyue connections mascot + 3-step "连接建立流程" flow card using `JoyJoinIcon` dashed connectors; `WelcomeGiftCard` integrated into profile-review, backed by `GET /api/user/welcome-coupon`; event-ticket-payment verification polling fallback shortened to 20 s (20 × 1 s); event-type tail illustration upgraded to v2 full-bleed footer vignette (`aspectFill`, CSS top fade, 4 s load timeout fallback to barcode); percentage coupons that compute a zero discount (e.g., WELCOME50 under ¥0.01 test pricing) are skipped at payment creation; CI mini-program build defaults to staging API. Plus 2026-06-30 (Pool-registration terminal-state visual parity: `PoolRegistrationAlreadyJoined` and `PoolRegistrationSuccess` now render `EventSummaryCard` so the post-registration event summary matches the "我的足迹" `FootprintOracleCard` design. The card is read-only on confirmation screens (no hover/tap hint/navigation). Terminal states are wrapped in `ScrollView` for small-screen safety; primary CTAs include haptics and analytics (`registration_terminal_state_view`, `registration_terminal_cta_tap`, `registration_terminal_notify_tap`). Notification enable button shows loading/disabled states. `EventSummaryCard` title uses `keep-all` + 2-line clamp. Matched-state shimmer migrated to GPU-safe opacity pulse. Shared `getSystemReducedMotion()` helper added to `src/lib/utils/accessibility.ts`.) Plus 2026-06-28 (Squad unboxing card-deck reveal: replaced member grid with fanned `SquadDeckStage` + `TeammateCard` / `TeammateCardDetail`, stacked card-back pattern asset, Xiaoyue `coachGuide` tap cue, `JoyJoinIcon` gift thumb in drag-reveal ribbon, card-back pattern preload on shaking state, `MissingArchetypePlaceholder` fallback; pool-group-detail uses localized archetype names and copy-address-for-navigation. Docs-sync update). Plus 2026-06-27 (Docs sync: shared `api.ts` split into domain modules under `packages/shared/src/api/*.ts`; admin user CRUD overhaul routes; landing auth gate now shows a spinner, 2.5s sticky timeout, and offline banner; personality-test slider questions submit from the next button). Plus 2026-06-23 (CDN asset cleanup: removed unused `chemistry-badges`, `ui`, legacy `archetypes`, `matching-heroes`, `pages/onboarding`, and `static/assets` directories from production CDN; added missing `reaction`/`reveal`/`achievement` icons and archetype head/PNG fallback copies; `cdn-asset-manifest.json` is now the source of truth. Event-ticket-payment supervisor optimization pass: ticket-card redesign, surfaced plan selector, auto-coupon "悦仔见面礼" stub, full payment-funnel analytics, trust/refund signals, return-context and sold-out gating, new Lovart hero banner. Shared production CDN strategy adopted: staging and production builds both load assets from `https://joyjoinapp.com/static`; removed `/var/www/cdn-staging`. Upload script fixed to resolve symlinked source directories before rsync. Plus 2026-06-16 Rating-face integration + phase-icon tier fix: restored `expression`/`rating-faces` WebP assets bundled locally, `RatingFace` component used in event-feedback, `phase` added to `CDN_ICON_TIERS`, icebreaker session preloads `ICEBREAKER_PHASE_EMBLEM_ASSETS`, `RatingFace.test.tsx` asset gate, main package ~1.87 MB zip). Plus: 2026-06-15 (Interest-heat picker full-marks push: bundled category-icon wiring, `category-social`/`话题` asset replacement, `usePreloadCategoryIcons`, milestone celebration toasts, archetype-aware Xiaoyue coach copy, 3-segment tier indicator, layered footer "heat story", offline submit guard, reduced-motion support; audit scores: 情绪价值 24/24, Frontend Design 20/20, Completeness 44/44, Performance PASS). Plus: 2026-06-17 (Intent selection consolidation: `JoyJoinIcon tier='intent'` wired across pool-registration, onboarding essential-data, profile-review and edit-profile; shared `toggleIntentValue()` helper in `@shared/constants` with unit tests; new reusable `IntentCard` component; edit-profile label changed to「社交期待」; pool-registration completion pill at cap; removed dead `PoolRegistrationFlowSteps`/`TerminalStates` files). Plus: 2026-06-11 (CityPickerSheet full-marks polish: Xiaoyue mascot header, accessibility — 88rpx touch targets + aria-labels + role=dialog, scroll-to-selected, 9 analytics events, strict 8rpx grid, haptics map). Discover full marks push: modals-at-root-level pattern, GPS auto-filter, GPS-denied pill, CityUnlockBanner removed, 8 pending SZ districts, Xiaoyue mascot in explore header, $color-overlay token, geo_auto_filter analytics). Plus: 2026-06-10 (ProfessionChatOverlay stability + polish round: stable scroll anchor replaces `bottomAnchorKey` preventing keyboard scroll-reset; confidence-based follow-up threshold for AI hints; empty hint guard; inline `max()` padding styles removed from input bar + CTA; reveal card moved inside ScrollView for keyboard-safe auto-scroll; legacy path parity guards; `profession_chat_keyboard_opened` + `profession_chat_reveal_card_viewed` analytics; `SMART_PROFESSION_V1_ENABLED` → `SMART_PROFESSION_ENABLED`; `AbortController` → `sendGenerationRef` dedup). Plus: 2026-06-08 Personality Test fundamental fixes round; 2026-06-08 ProfessionChatOverlay polish; 2026-06-07 audit fixes; 2026-06-06 custom tab bar stability fix; 2026-06-05 polish pass. Plus: 2026-06-13 (device-tier iOS heuristic fix, XiaoyueSpriteAnimator local WebP fallback, personality-test slider badge CSS-custom-property removal, canvas-poster CDN-URL enforcement, landing-page discover routing fix, completing-phase error returns to testing).
> Plus: 2026-06-15 (Profile tab social-passport redesign: gradient-ring archetype avatar, 3 stat cards with count-up + empty CTAs, milestone badges, 2-column menu grid, pull-to-refresh, stat-tap mascot reaction, spring entrance; `POST /api/analytics/profile` whitelisted events; shared `useCountUp` hook).
> Plus: 2026-06-17 (Pool-registration terminal-state extraction: loading/empty/already-joined/success states moved to `PoolRegistrationTerminalStates.tsx`; new `useReactionTimer` hook (retired 2026-08-05 — replaced by the mascot section's sprite-state machine); restored `100vh` fallback before `100dvh` per `AGENTS.md §4`; fixed `design-audit.mjs` regex; removed PII from registration log; fixed duplicate React keys in `XiaoyueLetterCard` reasons.)
> Plus: 2026-06-17 (Profile-linked subpackage migration: `edit-profile`, `rewards`, `invite`, `terms` moved to `pages/profile-linked/*`; preloaded from `pages/profile/index`; routes updated in `onboardingRoutes.ts`. Profile tab UI fixes: icons migrated to `JoyJoinIcon` with new local `ui` tier, scroll trap fixed, archetype avatar replaces placeholder, sub-screens aligned with shared tokens/page-gradient-bg. Share-card lazy loading: `generateProfileSharePoster` dynamically imported on first tap with `ShareCardShimmer` loading state. Wow-elements polish: menu row press feedback, CTA active scale, mascot entrance, share-card shimmer gated by reduced-motion/degradation. `usePageTTI` hook added for cold ≤2000 ms / warm ≤800 ms budgets on migrated pages.)
> Plus: 2026-06-18 (Post-audit hardening: native tab bar hides on non-tab routes via `TAB_BAR_PAGE_PATHS`; landing `PhaseIconCarousel` and personality-test slot animation are mandatory defaults with no reduced-motion/low-end fallback; `useMiniRevealMotion` honors only OS `reduceMotion`; Discover hero banner is local-first bundled WebP with CDN fallback; Discover logs shell-empty vs shell-failed diagnostics and shows a stable subtitle while registrations load; CSS-drawn chevron replaces unmapped emoji; HeroPromoBanner skeleton uses transform-based composited shimmer.)
> Plus: 2026-06-18 (Lovart 5×5 status/UI icon integration: cropped a 2048×2048 Lovart grid into `status-icons`, `ui`, and `info-labels`, plus six new `reaction-icons`; registered 25 emoji→icon mappings in `emojiToIconMap.ts`; added unambiguous tier fallback so dynamic emojis resolve when only one tier contains them; `StatusCard` now passes default tiers for empty/error/info icons; reaction assets uploaded to production CDN and verified HTTP 200.)
> Plus: 2026-06-18 (Asset-pipeline hardening: interest taxonomy v2.0 + 48 illustrated interest cards (`images/interests/*.webp`) live on CDN; `TARO_APP_CDN_BASE_URL` guaranteed at build time via `config/index.ts` default + CI fallback; mascot sprite bundle reduced to 6 core local states with 14 CDN-only; `status`/`ui`/`semantic` icon tiers stripped of `@3x` at build to bring compressed main package to ~1.88 MB.)
> Plus: 2026-06-23 (Events tab final polish: `JoinedEventCard` replaces `RichListCard` with archetype-family gradients, status pulse pill, countdown, display-font date, past-state recession, and GPU-safe entrance animation gated to first 6 cards and `useDeviceTier`; empty state uses Xiaoyue `coachGuide`; long lists fall back to `VirtualList`; tab hit targets ≥88rpx. Native custom tab bar cleanup: removed orphaned sliding-pill geometry/CSS and hardcoded `selected === 4`, replacing it with `CENTER_TAB_INDEX`; active state is now a per-item background highlight. Design-audit cleanup: removed remaining `height: 100vh` fallback lines from landing, personality-test, and results pages so guardrails reports 0 errors.)
> Plus: 2026-06-23 (Pool-registration Step 3 alignment: Step 3 now uses `XiaoyueCoachCard` to match Steps 1/2; read-only summary shows only budget + intent; chip visual weight reduced; Step 3 eyebrow derived from flow labels (`饭局` → “细节”, `酒局` → “氛围”); `reduceMotion` class gates step entrance animations; mascot `celebrate` sprite fires once on first detail selection (replaced 2026-08-05 by the mascot section's one-shot `nod` reaction); `hasAnyDetailSelection` helper handles `undefined` alcohol-comfort deselect correctly.)
> Plus: 2026-06-23 (Landing page and personality-test refactor: locked `100dvh` viewport with `disableScroll: true` on both surfaces, inner `ScrollView` ports for intro/question content, `ResponsiveSpacer` on landing to keep the CTA reachable on short phones, and reduced-motion gating for CTA glow/auth-hint entrance. Personality-test main file was split into `PersonalityTestIntro`, `PersonalityTestQuestion`, `PersonalityTestPreloadLayer`, `PersonalityTestCompletingError`, and shared `types.ts`, bringing `index.tsx` below the 1200-line frontend threshold; orphaned `personalityTestHelpers.ts` removed.)
> Plus: 2026-06-26 (Discover GPS fallback: when WeChat GPS permission is denied or times out, `pages/discover/index` calls `POST /api/geo/ip-locate` for city-level cluster detection via Tencent Maps WebService (`TENCENT_MAP_KEY`).)
> Plus: 2026-06-27 (Landing auth gate UX: `useAuthGate` exposes `isFetching` so returning cached users see a spinner; timeout reduced to 2.5s with sticky timeout banner and explicit retry/dismiss; offline startup surfaces its own banner. Personality-test slider questions now resolve the nearest option when the user taps the next button.)
>
> **Non-replacement note:** This document does **not** replace [`../PRODUCT_REQUIREMENTS.md`](../PRODUCT_REQUIREMENTS.md) or [`../apps/mini-program/README.md`](../apps/mini-program/README.md). It is a compact product-to-code bridge for the live WeChat mini-program surface.
> **Authority chain:** 1. [`../PRODUCT_REQUIREMENTS.md`](../PRODUCT_REQUIREMENTS.md) for product canon and terminology. 2. [`../apps/mini-program/README.md`](../apps/mini-program/README.md), [`../architecture/current-state.md`](../architecture/current-state.md), [`../systems/onboarding-flow.md`](../systems/onboarding-flow.md), and [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md) for runtime ownership and flow rules. 3. [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts), [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts), [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts), [`../packages/shared/src/README.md`](../packages/shared/src/README.md), [`../packages/shared/src/schema.ts`](../packages/shared/src/schema.ts), and [`../apps/server/src/README.md`](../apps/server/src/README.md) for active route, contract, and backend truth.

## 1. Purpose and scope

Use this document when you need the current mini-program page map, the active user journeys that matter on WeChat, the shared-contract boundary with server and shared packages, or the admin actions that visibly affect mini-program users.

This document intentionally stays shorter than the full product canon. It summarizes the live mini-program surface and links back to canonical files instead of re-stating full PRD, architecture, onboarding, or schema detail.

Out of scope:

- Full product requirements, terminology rationale, and long-form feature history. Use [`../PRODUCT_REQUIREMENTS.md`](../PRODUCT_REQUIREMENTS.md).
- Low-level Taro and WeChat platform primitives. Use [`../apps/mini-program/README.md`](../apps/mini-program/README.md) and, when needed, [`../reference/wechat-mini-program-reference.md`](../reference/wechat-mini-program-reference.md).
- Full table definitions. Use [`../packages/shared/src/schema.ts`](../packages/shared/src/schema.ts).

## 2. Product canon quick reference

The mini-program is the **launch-primary** client for the current JoyJoin track, but it still follows the shared product canon in [`../PRODUCT_REQUIREMENTS.md`](../PRODUCT_REQUIREMENTS.md).

Current product constraints that directly affect the mini-program:

- Canonical bottom navigation intent is **发现** -> **足迹** -> center action icon -> **连接** -> **我的**. On the mini-program these anchors map to [`../apps/mini-program/src/pages/discover/index.tsx`](../apps/mini-program/src/pages/discover/index.tsx), [`../apps/mini-program/src/pages/events/index.tsx`](../apps/mini-program/src/pages/events/index.tsx), the center action routing logic, [`../apps/mini-program/src/pages/connections/index.tsx`](../apps/mini-program/src/pages/connections/index.tsx), and [`../apps/mini-program/src/pages/profile/index.tsx`](../apps/mini-program/src/pages/profile/index.tsx).
- Use **连接** in current copy and documentation. **圈子** is legacy wording and should not be reintroduced as the active tab name.
- Use **权益** for user-facing entitlement language. Do not reintroduce **会员** as the active user-facing product term.
- Active value-first onboarding remains: personality test -> results -> auth gate -> authenticated onboarding -> discover. The canonical flow reference is [`../systems/onboarding-flow.md`](../systems/onboarding-flow.md).
- After authentication, onboarding progression is server-owned through `nextStep` from `/api/auth/user`, not reconstructed locally on the client. See [`../systems/onboarding-flow.md`](../systems/onboarding-flow.md) and [`../architecture/current-state.md`](../architecture/current-state.md).

## 3. Mini-program application structure

The live mini-program is a Taro 4 plus React 18 app under [`../apps/mini-program`](../apps/mini-program). Its page inventory is registered centrally and split across a main package plus subpackages for onboarding, pool registration, matching status, the in-event icebreaker, and the gathering-room pre-event anteroom.

| Layer | Source of truth | What it owns |
| --- | --- | --- |
| App shell | [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts) | Main-package page list, subpackage registration, preload rules, custom tab bar, and window defaults. |
| Page registry | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | Canonical route constants, page paths, subpackage membership, and preload entry points. |
| Main package | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | Root entry, tabs, payment, profile, event, and compatibility routes that must stay in the main package. |
| Onboarding subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/onboarding/*` routes for value-first entry and post-auth onboarding steps. |
| Pool-registration subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/pool-registration/index` for pool sign-up; success state uses a CDN ceremony hero. |
| Matching-status subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/matching-status/index` for match waiting and reveal states. |
| Icebreaker-session subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/icebreaker-session/*` routes for the in-event social icebreaker. |
| Gathering-room subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/gathering-room/index` for the pre-event online anteroom after matching. |
| Profile-linked subpackage | [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | `pages/profile-linked/*` routes for edit-profile, rewards, invite, and terms; preloaded from `pages/profile/index`. |
| Request and auth bootstrap | [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts) | Taro request transport, WeChat login helpers, `/api/auth/user` bootstrap, and payment verification request plumbing. |
| Runtime workspace rules | [`../apps/mini-program/README.md`](../apps/mini-program/README.md) | Build commands, package-loading rules, native custom tab bar ownership, and launch-primary client guidance. |

Current package split:

- **Main package:** discover, tabs, payment, profile, event-detail, event-coordination, and compatibility entry routes.
- **Onboarding subpackage:** `pages/onboarding` root with personality test, results, auth gate, essential data, extended data, and profile review.
- **Pool-registration subpackage:** `pages/pool-registration` root with the pool sign-up page; free-registration success uses a CDN ceremony hero.
- **Matching-status subpackage:** `pages/matching-status` root with the match waiting / reveal page.
- **Icebreaker-session subpackage:** `pages/icebreaker-session` root with the in-event icebreaker and tier selector.
- **Gathering-room subpackage:** `pages/gathering-room` root with the pre-event online anteroom; entered from `pool-group-detail` primary CTA or `squad-unboxing` post-confirm-attendance redirect.
- **Profile-linked subpackage:** `pages/profile-linked` root with edit-profile, rewards, invite, and terms; preloaded from `pages/profile/index`.
- **Preload rules:** `pages/index/index` preloads the onboarding subpackage; `pages/event-detail/index` and `pages/events/index` preload the pool-registration subpackage; `pages/profile/index` preloads the profile-linked subpackage.

## 4. Page inventory

This inventory is derived from the registered paths in [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts), the app shell in [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts), and the page implementations under [`../apps/mini-program/src/pages/`](../apps/mini-program/src/pages/).

### Entry and auth

| Page path | Role | Purpose | Notes |
| --- | --- | --- | --- |
| `pages/index/index` | Root entry | Mascot-led landing page (Xiaoyue hero, staggered entrance, BondingCloud particle animation, **icebreaker phase carousel**) for signed-out users; redirects signed-in users to the server-owned `nextStep`. Reads `invitationCode` from URL params and passes as `referralCode` to login API for invite attribution. | Uses `useStaggerMount` + `_stagger.scss` for entrance animation. `BondingCloud` with benchmark-aware particle count and reduced-motion support. **`PhaseIconCarousel`** renders 6 phase icons in a 3D turntable with auto-rotate (1.3–2.6s organic intervals), swipe gesture, direction randomization, cycle shuffle, birth rotation, glow pulse, and box-shadow depth (GPU-composited). The carousel is the mandatory default experience; no reduced-motion or low-end static-grid fallback. Per-icon error placeholders remain. **Performance:** `backface-visibility: hidden` + `translateZ(0)` forced compositing; `will-change` gated to active playback only; timer self-schedules to avoid effect churn; `useDidHide` clears all timers on app background; iPhone model regex + Android version fallback for tier detection. Legal-gated primary CTA with shake feedback; secondary returning-user login CTA. Haptics on all tap surfaces. Full exit animation coverage. **Cold-start redirects (2026-06-08):** `AutoLoginBridge` silently re-authenticates returning users; guests with an incomplete anonymous assessment are `reLaunch`ed back to `personality-test` instead of being stranded on landing. **Returning mid-onboarding users** are routed to the welcome-back screen (`pages/onboarding/welcome-back/index`) where they can see their current step, choose to continue, or restart onboarding (via `POST /api/auth/onboarding/restart`). This prevents dumping users directly into forms they may not remember starting (e.g., after deleting the mini-program). The welcome-back `seen` flag resets after 7 days because WeChat storage persists across deletion. **Navigation safety:** 5s timeout resets CTA loading state if subpackage download hangs. **Continue-mode CTA (2026-06-13):** returning authenticated users see `进入发现页` (when `nextStep='discover'`) and are routed directly to Discover, or `继续完善档案`; guests with an incomplete anonymous session see `继续完成测试`. The CTA sets `isPageExiting` before navigation and `useResetOnShow` clears it on swipe-back/foreground so the ellipsis spinner never survives.
| `pages/index/index?auth=logout\|expired` | Returning-user auth | Landing `loggedOut` CTA state (retired `pages/login/index` 2026-09-01): welcome-back copy, single 微信一键登录 CTA, settled mechanism strip. Reads `invitationCode` from URL params and passes as `referralCode` to login API for invite attribution. | Uses `Taro.login()` via [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts). Both `POST /api/auth/wechat/login` and `POST /api/auth/wechat/login-with-test` now accept `referralCode`. |

### Onboarding subpackage (`pages/onboarding/*`)

| Page path | Role | Purpose | Notes |
| --- | --- | --- | --- |
| `pages/onboarding/onboarding/index` | Redirect hub | Receives `nextStep=onboarding` and forwards immediately to the actual server-owned onboarding step. | Compatibility shell, not a standalone form step. |
| `pages/onboarding/personality-test/index` | Onboarding step | Runs the adaptive personality assessment with anonymous pre-auth support. | Value-first entry surface. **Intro mascot (2026-06-08):** The intro phase renders the mascot via `<Image src={getIntroStaticAsset()}>` (CDN animated WebP) with `onError` fallback to static WebP and `reduceMotion` detection via `Taro.getSystemInfoSync`. A pulsing placeholder renders while loading; the image fades in smoothly. This replaces the previous `XiaoyueSpriteAnimator state='intro'` sprite-sheet approach which caused jerky frame-by-frame animation. **Testing-zone mascot (2026-06-04):** Zone C renders the high-res 1190×1190 CDN WebP for the current question (chosen via `resolveStaticMascotExpression` keyed on `isSubmitting` + `questionType` + milestone/commentary flags) and only switches to the animated `XiaoyueSpriteAnimator state='thinking'` for the brief submit-to-next-question window. `transitionMs={0}` on the testing-phase sprite animator eliminates crossfade overlap that contributed to screen glittering. Avoids pixelating the small 200×200 sprite frame. **Question transition (2026-06-08):** `QuestionTransition` uses `key={questionId}` remount to restart the CSS entrance animation on every question change, eliminating the 17ms flash caused by the old `setAnimating(false)` → `setTimeout(...true, 17)` pattern. **Halfway milestone (Lovart Batch D D3, 2026-06-04):** hero badge `quizHalfway` (112rpx) appears when `progressPercent >= 50` and persists for the rest of the testing phase, anchored directly under `<SegmentedProgress>`. 0.36s stage entrance + 0.6s halo settle + 9 CSS-only confetti particles (star/sparkle/dot/ribbon) staggered 0.04–0.68s. Copy: `半程已过` / `走到一半了，继续走～`. Fires `logInfo('[PersonalityTest] halfway milestone reached')` and `analytics.interaction('personality_test_halfway_milestone_reached', { answered, estimatedTotal })` exactly once via `halfwayShownRef`. `aria-label='测验已完成一半，半程已过，继续加油'`. Under `prefers-reduced-motion: reduce` confetti freezes at final state (opacity 0.85, scale 1) instead of disabling — preserves the celebratory feel. **Answer-echo loading state (2026-06-08):** On answer tap, a "Soft Echo + Whisper Pulse" renders as an **absolute-positioned overlay** on top of the answer area — the answer area itself remains mounted underneath at all times, fixing the "missing option buttons" bug. Ghost card echoes the exact choice at 58% opacity, single thin brand-colored shimmer sweep (2.4s), Xiaoyue icon + "悦仔收到了，正在分析…" caption. 220ms exit animation before overlay disappears; next question update is applied atomically after exit. Kill switch: `personalityTestEchoEnabled` (DB-backed feature flag, default `true`). Respects `prefers-reduced-motion: reduce` and suppresses shimmer on low-end devices. Analytics: `personality_test_echo_shown`. **Speech bubble (2026-06-08):** `TypewriterText` now accepts `numberOfLines` (passed through to `<Text>`); the speech bubble uses `numberOfLines={3}` plus `word-break: break-word` to prevent text overflow. `MascotQuestionHeader` banner has `overflow: hidden` to ensure `numberOfLines={2}` clamp works reliably in WeChat flex layout. |
| `pages/onboarding/personality-test/results/index` | Onboarding step | Reveals the archetype result via slot-machine + silhouette-sparkle animation, then shows a hero card with **typicality badge** (`典型`/`非典型` prefix + archetype name in `accentText` colour), rarity indicator, nickname, and chemistry chips. **六维图 RPG radar chart**, Xiaoyue speech-bubble analysis, collectible holographic card with touch-drag tilt, and share/poster generation. | Includes progressive disclosure ("查看悦仔完整解读" premium pill CTA → detail modal with full AI analysis + **10-block segmented trait bars** + partner matches), hero-card skeleton loading, inline poster retry on failure, bridge-stage skip affordance, graceful fallback indicator, replayable reveal, and native share hooks. Slot animation is the mandatory default; it is skipped only when the server-driven `personalitySlotAnimationEnabled` feature flag is false. Slider badge position is returned as an inline `transform` (CSS custom properties removed because WeChat runtime support is inconsistent). Canvas poster (1080×1560) uses Pokémon-card compact layout with hero art, rank strip, trait bars, top match chips, energy bar, 2-column skill grid, and "限量氛围版" holo stamp. Archetype image is pre-resolved before generation, shared between portrait and square poster. Transient failures auto-retry once before surfacing error toast.
| `pages/onboarding/personality-test/results/index` (inline login) | Onboarding step | Results page triggers inline WeChat login and imports anonymous answers after successful auth. | Uses `/api/auth/wechat/login-with-test`. The standalone auth-gate page was removed in 2026-05. |
| `pages/onboarding/essential-data/index` | Onboarding step | Collects the essential profile fields required before users can continue deeper into onboarding. Includes a Xiaoyue-guided conversational overlay for free-text profession input that auto-classifies professions and generates AI-reveal reactions. | Post-auth step owned by `nextStep`. **Profession input (2026-06-07 hardened + 2026-06-08 audit round):** `ProfessionChatOverlay` is dual-mode (AI-driven via `POST /api/inference/understand-profession` + 220-keyword fallback). **Archetype-aware opening:** When user archetype is known, opening line is personalized (`像你这样的[archetype]，平时是做什么的呀？`) and expression anticipation maps profession keywords to Xiaoyue face before API responds. **Server archetype bridge:** Response includes `archetypeContext`; fallback reactions incorporate trait descriptors for warmer copy. **Timeout-aware fallback:** API timeout is 14s; on timeout abort, overlay surfaces a local keyword-matched reaction instead of silently returning. Analytics `profession_chat_classification_fallback` includes `timedOut` flag. Send quota is refunded on failure so user can retry. **Inline retry:** Fallback messages show a tap-to-retry hint directly in the bubble (`没识别准确？点击重新分析`) with 88rpx tap target. **Max-send guard:** 5 sends per session; exceeded shows banner `已达到最大重试次数，先继续吧～` with auto-dismiss after 4s. **Short-input hint:** When input < 3 chars, shows encouragement `多写一点，悦仔才能更懂你～`. **Progressive thinking labels:** `悦仔正在理解你的职业背景…` at 800ms and `还在思考中，马上就好~` at 2800ms. **Race-safe timer cleanup:** `thinkingTimersRef` array is cleared on unmount, new send, success, and catch paths. Added echo suppression for low-quality AI hints. **Confidence-based follow-up (2026-06-10):** `buildReactionHint` returns empty string when confidence >= 0.85 or source is non-AI; otherwise generates one clarifying question via `generateClarifyingQuestion` with 1500ms timeout. Frontend guards with `(data.reactionHint ?? '').trim()` + `if (hintText)`. **Offline detection (live):** `Taro.onNetworkStatusChange` monitors connectivity throughout the session; offline banner persists at bottom and auto-dismisses on reconnect. Device-tier gating (`profession-overlay--low-end` class) disables entrance animations. GPU safety: `will-change` removed from message/sparkle/tag-wrap. Preload uses off-screen 2rpx pattern (not zero-dim). Haptic feedback on send, confirm, skip, and reveal. Analytics events: `profession_chat_echo_suppressed`, `profession_chat_classification_success`, `profession_chat_classification_fallback`, `profession_chat_offline_blocked`, `profession_chat_skipped`, `profession_chat_retry_tapped`, `profession_chat_max_send_reached`, `profession_chat_keyboard_opened`, `profession_chat_reveal_card_viewed`. Stable scroll anchor uses `scrollTrigger` counter + static `id='bottom-anchor'` (replaces `bottomAnchorKey` that reset scroll on keyboard). **Accessibility:** Overlay has `role="dialog"` + `aria-modal="true"`; reveal card has `role="region"` + `aria-label="职业分析结果"`; chat ScrollView has `aria-live="polite"`; per-message `expressionId` varies by tone (`homeWelcome` on skip). **Performance:** Message list memoized with `useMemo` and wrapped in `<CustomWrapper>`; `will-change` auto-removed after entrance animation via `onAnimationEnd`; expression assets preloaded via hidden `<Image>` nodes; request deduplication via `sendGenerationRef` counter; `requestAnimationFrame` for retry timing; `cursorSpacing={32}` on Input. Inline `max()` padding styles removed from input bar and CTA. Reveal card moved inside `<ScrollView>` for auto-scroll on keyboard. Legacy path parity: `handleSendLegacy` enforced with same short-input, debounce, max-send, offline guards. Gated by `SMART_PROFESSION_ENABLED` (default true). **Accessibility & performance (2026-06-13):** `prefers-reduced-motion` is detected via `Taro.getSystemInfoSync` and applied as a `profession-overlay--reduce-motion` class that suppresses entrance/exit animations, typing dots, sparkle bursts, and keyboard-slide transitions. The keyboard-aware input bar uses `transform: translateY(-keyboardHeight)` to stay on the compositor and avoid layout thrashing on every keyboard frame. |
| `pages/onboarding/extended-data/index` | Onboarding step | Interest-heat picker. Tap-to-cycle each topic through 0 → 1 (感兴趣) → 2 (很热衷) → 3 (必聊项) → off; 3–10 selections required. | Canonical extended-data step. **Category icons:** `JoyJoinIcon tier="category"` resolves bundled `src/assets/icons/category-icons/` (food 🍜, play 🎮, sports 🌿, culture 🎭, life 🏠, growth 💡) via root-relative `/assets/icons/category-icons/` paths (no runtime `require()` in subpackages). Locally bundled icons load eagerly by default to prevent emoji fallback in subpackages. `usePreloadCategoryIcons` pre-warms them before the grid renders. **Interest taxonomy v2.0.0** restructured categories around meeting scenes: 美食小酌 / 聚会玩乐 / 运动户外 / 文艺现场 / 生活美学 / 思想成长. **Heat guidance:** three-step visual legend (感兴趣 → 很热衷 → 必聊项) + subtitle `轻点加入 → 再点升温 → 三档成为必聊项` explains the multi-tap gesture up front. **Ghost-tap demo:** a CSS-only loop on the first card shows the 0→L1→L2 progression until the user's first real tap, demonstrating the gesture before any interaction. **Per-tap coachmarks:** anchored bubbles (`再点一次增强热度` / `再点一次设为必聊项` / `再点取消必聊项`) appear after each tap, each with a pulsing tap glyph. **Tier indicator:** 3-segment vertical bar on each selected card. **Milestone toasts:** centered celebration for unlock (≥3 selections), first L3, and all 6 categories. **Archetype-aware copy:** Xiaoyue coach line and footer story pill personalize around `user.archetype`. **Footer summary:** layered "heat story" with thermometer (fills by selection-count semantics), combined stat, total-heat value, top selection chips, and dynamic story pill. **Offline guard:** `Taro.getNetworkType` blocks submit with warm toast when disconnected. **Reduced motion:** CSS animations suppressed via `@media (prefers-reduced-motion: reduce)`. **Accessibility:** `role="button"`, `aria-pressed`, `aria-label` per card; `aria-live="polite"` on hint/milestone toasts. |
| `pages/onboarding/profile-review/index` | Onboarding step | Single premium "admission poster" card summarizing archetype, profile mini-cards, intent chips (`JoyJoinIcon tier='intent'`), interest heat map, and an AI-generated social tagline. Includes a Xiaoyue analyzing interstitial, holographic shimmer, and a scroll-aware floating CTA. | Now also captures an optional 1–100 character bio (`一句你的社交签名`) via a textarea with live counter and `aria-label`; validated and persisted by `POST /api/profile-review/complete`. A non-empty bio contributes +10% to profile completion. **Welcome gift (2026-06-30):** on first profile-review view, calls `GET /api/user/welcome-coupon` to claim (or re-fetch) the lifetime welcome coupon and renders `WelcomeGiftCard` above the admission poster with the coupon discount, `悦仔见面礼` branding, and a tap CTA to Discover. **Flow 1 intro overlay — removed (2026-08-26, onboarding PR-5):** the once-per-user `joyjoin-intro` overlay no longer plays after the UnboxingCeremony (single-ceremony rule; play-mode explainer moved to the Discover arrival coachmark — `docs/agent-context/flow-animation.md`). **Radar summary card (redesigned 2026-08-03):** after the coupon/gift card settles, renders `ProfileReviewInviteCard` — a display-only profile summary with a full-bleed Lovart `invite-teaser.webp` banner and a "kindred radar" overlay (user archetype centered, 4 satellite archetypes popping in with stagger). No tap interaction; the primary CTA is the only completion path. Card reveal triggers a predictive `GET /api/shell/discover` prefetch via `PrefetchEngine` so Discover is warm on navigation. Analytics: `profile_review_invite_impression`, `profile_review_discover_prefetch_hit` (`profile_review_invite_tap` retired). Motion gated via `@media (prefers-reduced-motion: reduce)` and `useMiniRevealMotion`. |

### Tab and account surfaces

| Page path | Role | Purpose | Notes |
| --- | --- | --- | --- |
| `pages/discover/index` | Primary destination | Shows discoverable event pools with filters, fill state, and the main join entry path. | Launch-primary discovery surface. **OracleCard** (`apps/mini-program/src/components/discover/OracleCard.tsx`) — primary pool card with 6-layer visual hierarchy: L1 hero message (28rpx, $font-cn-display), L2 topline (pulse + countdown), L3 decision facts (40rpx date, 30rpx event type, 26rpx location), L4 title (26rpx secondary), L5 social-proof teaser (28rpx bold), L6 progress bar + CTA (32rpx black). Per-pool 4-branch narrative pivot (empty/rare/present/dominant). Full-pool disabled state with grey CTA + "已满员". High-chemistry celebration badge when ≥3 high-match archetypes. **Event-type corner vignettes (2026-06-23):** Lovart dining/drinks illustrations render top-right on `饭局`/`酒局` pools (and English aliases), CDN-only, lazy-loaded, and hidden on degradation-tier devices. **Corner registration-count badge (2026-07-01):** warm beige pill overlaps the vignette, showing real participant count when >0 and pool is not full. Event-type-aware copy: `饭局` → "X 人入座中", `酒局` → "X 人已入席", others → "X 位伙伴已加入". Count capped at `999+`; number color is contrast-safe archetype color when user's archetype is known, otherwise card family color. Hidden via `user.features.oracleCardCornerStatEnabled` (env `ORACLE_CARD_CORNER_STAT_ENABLED`, default `true`). Analytics: `corner_badge_impression` once per mount, `corner_badge_live_update` on count change while mounted; `pool_card_tap` includes `hasCornerCount` and `cornerCount`. Low-end device gating via `useDeviceTier()`. Kill switch via `enabled` prop (wire to `features.oracleCardEnabled`). Analytics: `pool_card_tap` (cardVersion: `oracle_v1`) includes `hasEventTypeVignette`. Haptics: light on tap. Accessibility: `role='button'`, `aria-label`, `aria-disabled`, 88rpx tap targets. Styles at `pages/discover/index.scss` with 8rpx-grid margins, `prefers-reduced-motion` gating, and WeChat-safe `rgba()` colour tokens. **Top-of-page hero:** `HeroPromoBanner`... Three copy variants (A/B/C) rotate by user archetype + `?promo=A` URL override. Local-first bundled WebP with CDN fallback on `onError` and bounded retry. **Copy hardening (2026-06-05):** static variants must not fabricate social-proof metrics (user counts, popularity percentages); use real query-backed numbers or omit the metric. Kill switch via `user.features.promoBannerEnabled` (env `PROMO_BANNER_ENABLED`). Analytics: `promo_banner_impression`, `promo_banner_cta_tap`, `promo_banner_image_error`, `promo_banner_image_retry`. See `apps/mini-program/src/components/HeroPromoBanner.tsx`. **Smart Regional Discovery** (2026-05-18): silent GPS detection → cluster-level proximity sorting (same cluster first, adjacent next, then time). Manual filter override via `LocationFilterDrawer` (bottom-sheet district selector) with 7-day TTL persistence. **Drawer scroll fix (2026-06-17):** `70dvh` replaced with `calc(1020rpx - $tab-bar-root-height)`; `<ScrollView>` uses inline `height: 100%`, `enableFlex`, and a `min-height: 0` flex parent. `catchMove` is placed on both the backdrop and the drawer surface to stop background-page scroll penetration while preserving internal ScrollView scrolling. Drawer sheet renders at `z-index: $z-modal` (`200`). District tiles render in a 2-column grid (136rpx min-height) with 28rpx names and compact top-right heat badges (`热门`/`活跃`/`即将开放`). Selected tile shows filled primary background + white checkmark. Header includes a 48rpx Xiaoyue mascot. Tiles use `role='button'`, `aria-pressed`, and descriptive `aria-label` including heat level. Heat-badge colours use SCSS token classes, not inline hex. Reduced motion is handled via both CSS media query and `useMiniRevealMotion`. Auto-relaxation banner when manual filter yields empty results. Geo-hint shows detected cluster. Analytics: `geo_detected`, `geo_failed`, `filter_auto_relax`. **Branded location pill (2026-06-05):** discover header pill now renders `$brand-gradient` by default with white text/icon for consistent brand presence. **Interaction polish (2026-06-02):** haptics on pool card tap, location pill open, and pull-to-refresh. **Persistent cache (2026-06-02):** pools data survives app kill via `Taro.setStorageSync` (4h TTL, 75KB cap). Background refetch shows `isFetching` shimmer indicator. Pull-to-refresh evicts persistent cache. **CTA guard (2026-06-05):** `HeroPromoBanner` always receives `onCtaTap={handleBannerCtaTap}`; the component internally disables the CTA when `openPools.length === 0` and shows toast `暂无开放活动`. CTA pill has `margin-bottom: 8rpx` to prevent boundary clipping. **Empty state (2026-06-05):** uses `StatusCard` with `tone='empty'` and Lovart `lovart-generic-empty.webp` hero illustration, with title, description, and a primary action (`去发现活动`, or `清除筛选` when a manual filter is active). **Error state (2026-06-05):** list fetch failures render `StatusCard` with `tone='error'`, a Lovart error illustration, and a retry CTA. See `apps/mini-program/src/components/discover/LocationFilterDrawer.tsx`. **GPS auto-filter (2026-06-11):** On first GPS success (`onLocationChange`), silently auto-selects detected district filter via `hasAutoSetFilterRef` (once per session). Tracks `geo_auto_filter` analytics event. **GPS-denied pill (2026-06-11):** When geolocation is denied/error, location pill shows "选择你的区域" (neutral text, no false city claim). **Modals-at-root-level (2026-06-11):** `LocationFilterDrawer` + `CityPickerSheet` rendered at `DiscoverPage` root level (outside `PageMorphWrapper`) to fix WeChat WKWebView `position: fixed` stacking-context bug where modals hidden by scroll. **Xiaoyue mascot in explore header (2026-06-11):** 40rpx `coachGuide` mascot image next to "探索体验" title for brand warmth. **CityUnlockBanner removed (2026-06-10):** gentle banner fully removed; only `CityUnlockFeedCard` remains. **8 pending Shenzhen districts (2026-06-11):** `packages/shared/src/districts.ts` now exports 3 clusters (nanshan/futian/external) with all 10 districts. Previously missing 8 districts added with `pending` heat level ("即将开放"). Heat level type includes "pending". **$color-overlay token (2026-06-11):** extracted into `apps/mini-program/src/styles/_variables.scss`, used by drawer + picker backdrops (replaced hardcoded `rgba(0,0,0,0.4)`).
| `pages/events/index` | Primary destination | Shows joined events split into upcoming and completed tabs. | User-facing label is `足迹`; page title is `我的足迹`. Primary data source is `GET /api/shell/events` (composite) with fallback to legacy `/api/events/joined`. **Venue-status alignment (2026-06-29):** the footprint card derives a user-facing `displayStatus` from `matchStatus`, `poolStatus`, `venueAssignmentStatus`, and `venueName`; venue is disclosed only when `displayStatus` is `confirmed`/`venue_unlocked`; raw `matched` shows event type + city/district. Terminal pool states (`completed`/`cancelled`) override the registration-level status. Shared `isJoinedEventTerminal()`/`JOINED_EVENT_TERMINAL_STATUSES` in `lib/utils/eventDisplay.ts` centralize terminal detection for card UI and event partitioning; `getJoinedEventDisplayDateTime` prefers `displayStatus` for `finalDateTime` resolution. **Interaction polish (2026-06-02):** haptic feedback on tab switch, `hoverClass` pressed state on tabs, skeleton loading. **Persistent cache (2026-06-02):** joined events survive app kill via `Taro.setStorageSync` (4h TTL, 75KB cap). Added pull-to-refresh (`usePullDownRefresh`) with cache eviction. Background refetch shows `isFetching` opacity-pulse indicator. **Lovart Batch D (2026-06-04, D1):** `firstEvent` celebrate tile (200rpx) shows the first time a user has a joined event; uses solid gradient bg (no dashed border) for celebratory feel. **Empty state (2026-06-23):** uses `StatusCard` with `tone='empty'` and Xiaoyue `coachGuide` mascot hero illustration plus a primary action button (`去发现`). **Error state (2026-06-05):** composite or legacy fetch failures render `XiaoyueEmptyState` with `emotion='sad'` and a retry CTA. Also wired to the legacy `pages/my-events/index` alias. **2026-06-23 final polish:** event cards render via `FootprintOracleCard` (`apps/mini-program/src/components/events/FootprintOracleCard.tsx`) through a legacy `JoinedEventCard` compatibility wrapper; the card uses archetype-family gradients, status pulse pill, segmented countdown, display-font date, location prefix, and past-state recession. **Two-rail layout (2026-07-07):** the card splits into a left body (status, title, date/time/location) and a 168rpx right rail (compact countdown, group-size hint, price, or a "待公布" placeholder); the Lovart corner vignette is clamped to the rail width to avoid overlapping body text. The rail itself is not `aria-hidden`; only the decorative `›` cue is hidden, and `FootprintOracleCard` supplies a `railAriaLabel` so screen readers announce the rail contents. Entrance animation is gated to the first 6 cards and disabled on degradation-tier devices; long lists fall back to `VirtualList`; tab hit targets are ≥88rpx; safe-area bottom spacing fixed. Skeleton and refresh animations use GPU-safe opacity pulse (no background-position shimmer). `status-chip` styling added for event status meta. Countdown pill colour changed from `$color-error` to `$color-secondary`. Shared helpers `formatEventDateTime`, `getCountdownText`, `getJoinedEventStatusLabel`, `isJoinedEventTerminal` in `lib/utils/eventDisplay.ts`. Analytics module `lib/analytics/eventsAnalytics.ts` tracks `events_view`, `events_tab_switch`, `events_card_tap`, `events_empty_state_cta_tap`, `events_pull_refresh`.
| `pages/connections/index` | Primary destination | Shows post-event connections with peer identity and event context. | Primary data source is `GET /api/shell/connections` (composite) with fallback to legacy `/api/my-connections`. **Connection cards (2026-06-16):** reuse `ProfileArchetypeHero` to show peer age range, city, bio, and a mutual-context line (`都在${city}` / `都是${archetype}` / `默契值 ${chemistry}`). Privacy-safe age ranges respect the peer's `ageVisibility` preference; empty bio hides the bio line. **Context-aware empty states (2026-06-18 / archetype main visual 2026-06-21):** server-driven `connectionsContext.mode` resolves to `no-events` (→ discover), `upcoming-event` (→ events, with event title), `feedback-pending` (→ event-feedback), or `feedback-complete` (celebration badge + success haptic). Each state explains the connection mechanism (join event → submit feedback → mutual selection → connection appears). When the user has a primary archetype, the archetype head icon replaces the Xiaoyue mascot as the main visual (circular crop, white border, shadow, accent halo) with a personalized subtitle prefix `作为{nameCn}的你，`; otherwise the mascot emotion sprite is shown. Empty states emit `connection_empty_state_impression` (including `display_mode: 'archetype_head_main' | 'mascot'`); CTA taps emit `connection_empty_state_cta_tap`. **Empty-state redesign (2026-06-30):** the no-data state renders a hero card with Xiaoyue `connections-empty` mascot, glow ring, decorative dots, CTA `去探索活动`, and a 3-step "连接建立流程" flow card (`参加活动 → 完成互动 → 建立连接`) using `JoyJoinIcon` emoji icons and dashed connectors. **Loading state (2026-06-05):** initial data fetch shows `XiaoyueEmptyState` with `emotion='waiting'`. **Interaction polish:** pull-to-refresh, CTA busy spinner + "跳转中…", haptics, reduced-motion support. **Viewport centering:** empty/loading wrapper height is computed from `Taro.getSystemInfoSync()` (no `100vh/100dvh`). **Error state:** composite or legacy fetch failures render `XiaoyueEmptyState` with `emotion='reassure'` and a retry CTA. Auth gate uses `useMiniPageGate` with a 4s force-release timeout to prevent stuck loading after app resume. |
| `pages/alang/*` | Feature subpackage | 正式街头盲盒 5 NPC × 3 幕；`pages/alang/event` 为首页，展示在线 NPC 与 15 格故事碎片收藏条；`pages/alang/archive` 为归档/仪式页；config/debug 与正式 15 幕重玩仅 strict non-production single-test。 | 正式链路由 `event` → `search` → `dialogue` → `result` → `archive`，高亮/专属小游戏完成后由服务端把幕完成与唯一故事碎片原子结算；碎片目录异常时不提交进度。结果卡保留到用户确认收下，归档仪式 24s 内可跳过。15 幕复测均从开场开始，使用独立 replay cursor、`replaySession` 和本地存储命名空间，不写正式进度或重复碎片。兼容阿浪链路仍以 `myProgress`/archive 为权威，搜索不返回目标坐标，Map 只显示用户；陪伴路线仅在显式点击后请求。R2 打磨见 `docs/deliberations/2026-08-26-flash-polish-strategy.md`，详见 [`../alang-prototype/implementation-map.md`](../alang-prototype/implementation-map.md) 与 [`../agent-context/alang-flash.md`](../agent-context/alang-flash.md)。 |
| `pages/profile/index` | Primary destination | V1.7 “我的”页：沿用现有暖白/紫色界面，12 人格透明像素伙伴、姓名/人格、真实潮流值、活动/连接/资料完成度、私人连续故事入口、我的形象、徽章、服务与设置。 | `GET /api/user/gamification` 和 `GET /api/shell/profile` 提供真实数值；`GET /api/equipment/me` 提供本人衣橱/保存形象。`profileRedesignEnabled=false` 选择简洁真实数据页，并停止 gamification/equipment/personal-story 专属请求。V2 纸娃娃资源：12 张基础身体 + 48 张初始装备层从各人格 `atlas-source.png`（2×3 穿着阶段格）做**角色差分提取**（下采样剪影粗对齐 + 头部精对齐，硬贴合质量门禁 ≤3%漏提/≤5%误捕）；大象/龟/猫暂用独立格适配。12 人格各一张 `fullStarter` 完整穿搭正式图（spider 保留 V1 图）。Profile 只合成 front 状态；`IdentityStageScene` 落日街道纵深舞台 `aspectFill` 替代旧网格底（场景失败退网格）。不使用紫色赛博/霓虹城市舞台。子包 WXSS 分裂已修复（`verify:subpackage-styles` CI 门禁）。**2026-07-26 视觉打磨**：舞台入口取消 scale 回弹、角色视觉锚定右下角、复制卡常驻 `ArchetypeHead` 人格色环 + 网格 emblem、暖色玻璃卡/紫装备条/三色语义统计卡、RPG 铭牌与 XP 流光动画。 |
| `pages/profile-linked/personal-story/index` | Profile-linked subpackage | 仅本人可见的连续故事：封面、最后更新时间、手动“更新故事”和从最早到最近的章节时间线；只有进入页面才展示更新状态。 | `personalStoryEnabled=false` 时 Profile/阿浪结果不进入该页，GET/POST/status 也在访问新表前关闭。开关为 `true` 后，POST 才从本人真实完成的阿浪/盲盒经历提取关键词并按一次经历一章追加；盲盒以本人的 group outcome 锁定分组，并额外要求本人已匹配、非测试未取消、活动已结束，且该活动本人的 `event_feedback.completedAt` 非空。MiniMax 主、DeepSeek 回退；provider 不可用或输出越界时不生成替代章，GET 仍返回既有章节。 |
| `pages/profile-linked/my-image/index` | Profile-linked subpackage | 我的形象：`IdentityStageScene` 落日街道纵深场景包裹 `PixelAvatar3D`/V2 合成器（`aspectFill`，场景资源 CDN-first，失败退旧网格底）。装备层从 `atlas-source.png` 差分提取，`fullStarter` 成套整图。角色身上点选槽位热点（2026-08-01 起移除五档伪 3D 转盘，恒定正面）；四个槽位 tab 带已装备圆点；换装 240ms 淡入（RM 降级）。沿用现有暖白/紫色 UI。 | 初始装备层自每人格 atlas 差分提取（硬贴合质量门 ≤3%漏提/≤5%误捕）。每件装备有一个裁边透明 layer 按 manifest placement/depth 放到身体上，另配独立 garment-only `thumb-v2` 方形产品图作为卡片/商店缩略图（2026-08-01 起由独立装备 sheet/atlas 单元格生成，不再含身体描边像素）。脱下只移除对应层，贴身背心与安全短裤不可脱。**子包 WXSS 分裂陷阱**：V2 composite 规则曾被打包进不可达 `three-avatar.wxss` → 11/12 人格白屏；页面 SCSS 必须显式 `@use` 所有组件样式；`npm run verify:subpackage-styles` CI 门禁已挂入 `taro-weapp-build.yml`。真实完成活动才产生永久手动 draw entitlement；池按 venue/mission，80/20 普通/稀有，第 4 抽当前池新品保底，重复转 10/30 碎片，商店 40/120 且没有支付路径。 |
| `pages/profile-linked/edit-profile/index` | Auxiliary destination | 2-step continuous-scroll profile editor: Step 1 "基础档案" (name, gender, birth year, city, hometown, education, bio) + Step 2 "活动画像" (profession via `ProfessionChatOverlay`, intent grid, interest tags with 3-tier tap-to-cycle). **Bio field (2026-06-16):** optional 1–100 character party signature (was social signature, renamed 2026-08-18) with live counter, optional clear, content-safety validation, and a live preview of `ProfileArchetypeHero`. **Intent field (2026-06-17 / icons enlarged 2026-08-05):** label「活动期待」(was 社交期待, renamed 2026-08-18); each option renders a 54rpx `JoyJoinIcon tier='intent'` with disabled/dimmed states and uses shared `toggleIntentValue()` for `MAX_INTENTS=3` cap logic. Save logic treats a pending clear as a real change so users can remove an existing bio. Features: field-level validation, scroll-to-error via `ScrollView scrollIntoView`, unsaved-changes guard (`Taro.enableAlertBeforeUnload`), skeleton loading, haptics, floating bottom CTA bar, reduced-motion support. **TTI instrumentation (2026-06-17):** uses `usePageTTI` with cold ≤2000 ms / warm ≤800 ms budgets. | Redesigned 2026-05-24 for cognitive continuity with onboarding. Moved to `pages/profile-linked` subpackage 2026-06-17 and preloaded from `pages/profile/index`. **Data contract:** `PATCH /api/profile` persists `bio`; the server normalizes whitespace, applies a 100-character ceiling, and runs content-safety gating before saving. |
| `pages/profile-linked/rewards/index` | Auxiliary destination | Shows coupons, gamification state, recent history, and redeemable items. | Reads shared rewards and coupon APIs. Moved to `pages/profile-linked` subpackage 2026-06-17. |
| `pages/profile-linked/invite/index` | Auxiliary destination | Shows referral code, invite link, reward tiers, and copy actions for friend invites. Features: WeChat share card via `useShareAppMessage`, pull-to-refresh, error state with retry, haptics on all interactive actions, safe-area spacing. | Uses shared referral stats contract (`getReferralStats`). Link generation produces mini-program deep link path (`/pages/pool-registration/index?invitationCode=...`). **Lovart Batch C (2026-06-04, C4):** `inviteCoBranded` ceremony hero (~200rpx square) anchors the share-card section, signalling "we are in this together" with a co-branded 悦仔 + invitee illustration. **v0.1 polish (2026-06-16):** header hero `inviteHeader` replaces the emoji above the title. Moved to `pages/profile-linked` subpackage 2026-06-17. |
| `pages/profile-linked/terms/index` | Auxiliary destination | Renders the current legal terms or privacy content with section focus support. | Backed by shared legal copy. Moved to `pages/profile-linked` subpackage 2026-06-17. |
| `pages/city-unlock/index` | Auxiliary destination | City unlock progress screen showing interested-city progress, share CTA, and activity feed. Entry from discover banner/feed-card or profile. | **City Unlock v0.1** (2026-05-19). Uses `xiaoyue-city-unlock` mascot asset. Share via `useShareAppMessage` with city-specific title. |
| `pages/center-hub/index` | Tab destination (center) | Dynamic hub for active events, pending registrations, and empty state. All center button taps route here via `switchTab`; internal CTAs then `navigateTo` detail pages. | Replaces `pages/center-tab-empty/index`. **Empty-state redesign (2026-06-30):** when no active registrations or events, renders a hero card with Xiaoyue `coach-guide` mascot, glow ring, decorative dots, CTA `去探索活动`, and a 4-step "活动流程" flow card (`报名成功 → 等待匹配 → 小队揭晓 → 确认出席`) with numbered indicators and dashed connectors. **Interaction polish (2026-06-02):** haptics on all CTA buttons. Loading state shows Xiaoyue `homeWelcome` mascot with bounce-in animation instead of dead text. Error state shows Xiaoyue `actionFailure` mascot for emotional support.

### Event, payment, matching, and in-event surfaces

| Page path | Role | Purpose | Notes |
| --- | --- | --- | --- |
| `pages/blind-box-payment/index` | Payment entry | Lets users buy entitlements or packs needed to continue the blind-box registration flow. **Mock mode:** when `MOCK_PAYMENTS=true`, server returns instantly-paid orders (`status: completed`, `mock: true`); client skips `Taro.requestPayment()` and goes straight to verification. | Current strongest reference for live mini-program payment mechanics. **Welcome coupon banner (2026-06-08):** Auto-finds `WELCOME50`/`WELCOME40` from available coupons and renders `FirstTimeCouponBanner` above the coupon list. Tapping auto-selects the coupon and shows "已选择优惠券" toast. Banner is hidden once a coupon is selected. Counter animation, confetti burst, archetype-aware copy, savings preview, and full accessibility.
| `pages/payment-verification/index` | Payment recovery | Polls payment status after the WeChat pay sheet returns and routes back to the right post-pay destination. | Uses pending-order context and shared payment status decisions. **Lovart Batch C (2026-06-04, C2):** `eventPaidConfirmed` ceremony hero (~340rpx) sits above the existing "支付成功" copy; `haptics('success')` fires on transition. Paired with `actionSuccess` Xiaoyue. |
| `pages/event-ticket-payment/index` | Payment entry | Premium ticket-card UI for paid event registration. Shows event summary as a single ticket surface (hero banner, notched perforation divider, meta grid, preference chips, price block, dashed coupon stub, event-type tail illustration with barcode fallback), inline plan selector (single / 3-pack / 6-pack), price summary, refund-policy link, trust marks, and a sticky WeChat Pay CTA with savings badge. Auto-selects the highest-value welcome coupon (`WELCOME50`/`WELCOME40`) on load, surfaces it as a "悦仔见面礼" gift stub with a stamp-reveal animation, and burns the coupon only on successful payment. **Zero-discount guard (2026-06-30):** percentage coupons that compute a zero-cent discount (e.g., `WELCOME50` when `TEST_PAYMENT_PRICE_IN_CENTS=1`) are dropped from the payment payload so server validation does not reject the order. Payment creation/verification is guarded by sold-out / already-registered / missing-return-context gates. Polls payment status inline and surfaces success/verifying ceremony states. Instruments the full funnel via `discoverAnalytics`: `event_ticket_payment_view`, `plan_switch`, `welcome_coupon_auto_applied`, `pay_start`, `pay_success`/`pay_cancel`/`pay_fail`/`pay_timeout`, `refund_policy_viewed`, `event_ticket_payment_abandon`, `ticket_terms_row_impression`, `ticket_inclusion_sheet_open`, `ticket_inclusion_sheet_close`, `ticket_tail_image_impression`, `ticket_tail_image_load_error`. | Added 2026-06-12. **「订座」仪式重构 (2026-08-17):** success state no longer uses the legacy `eventTicketSuccess` ceremony hero; it renders the shared `RegistrationSuccessCeremony` (`apps/mini-program/src/components/reservation/RegistrationSuccessCeremony.tsx`) backed by `ReservationTicket`. The paid variant shows a tear-off ticket stub and a CSS scale-in `已留座`/`已确认` seal; the seal landing fires `socialHaptics('socialCelebration')` exactly once. Verifying state still uses `paymentVerifying`. **Tail illustration v2 (2026-06-30):** full-bleed footer vignette (`750 × 280 px`, `aspectFill`, CSS top fade overlay) bleeds to the ticket-card edges; 4 s load timeout falls back to barcode decoration; degradation-tier devices skip the image. **Supervisor optimization pass (2026-06-17):** plan cards surfaced by default, full analytics coverage, refund/trust signals, CTA savings badge, return-context guard, sold-out/already-registered gating, improved welcome-coupon UX, and new `lovart-event-ticket-payment-hero-20260617-v1.webp` hero banner. **P2 icebreaker inclusion (2026-06-23):** ticket card surfaces a `含悦仔多环节破冰` chip; tapping the terms row opens `IcebreakerInclusionSheet`, a drag-to-dismiss bottom sheet that previews representative social-icebreaker phases (warmup, micro-challenge, lie-detective, personality-dice, group-mirror, undercover-word, quip-battle, speed-friending, recap). Includes system-back/swipe-back handling, scroll-top drag guard, reduced-motion gating, and server-whitelisted analytics events for impression, open, and close. **Ticket-tail asset pass (2026-06-30):** bottom of the ticket card renders an event-type-specific Lovart tail illustration (`lovart-event-ticket-tail-dining-20260630-v1.webp` / `lovart-event-ticket-tail-drinks-20260630-v1.webp`, 500×250 WebP, CDN-only, ~17–25 KB); degradation-tier devices or image load failures fall back to the original barcode decoration. Tail fade-in is gated by `prefers-reduced-motion`. **Polling fallback (2026-06-30):** payment verification polling now runs 20 attempts × 1 s = 20 s fallback window (was 24 × 1.5 s = 36 s); if polling times out, the client calls `POST /api/payments/:wechatOrderId/reconcile` to query WeChat Pay directly and fulfill the order if paid. **Success hero v2 (2026-07-01):** full-bleed warm ceremony illustration (`750 × 604 px`, `mode='widthFix'`) with Xiaoyue + 小太阳鸡 co-celebrating around a glowing golden ticket; bottom third dissolves into Warm Beige `#F5F1E8` via a 180rpx CSS gradient bridge so text reads as part of the scene. Page background `#F5F1E8`, stagger entrance (hero 0ms / title 150ms / subtitle 250ms / chip 350ms / CTA 450ms, total ≤900ms), one-shot confetti (≤16 pieces, 2–2.5s) gated by `prefers-reduced-motion` and degradation tier. State-aware subtitle switches between matching-in-progress and expectation-setting copy using `DEFAULT_MASCOT_DISPLAY_NAME`. New analytics: `event_ticket_payment_success_view`, `event_ticket_payment_success_cta_tap`. CTA double-tap guard with `switchTab` failure rollback and `useResetOnShow` for swipe-back safety. |
| `pages/event-detail/index` | Primary destination | Shows the detail view for a single event or event pool entry point. | Event-specific read surface. |
| `pages/event-feedback/index` | Primary destination | Collects post-event feedback in a merged 3-screen flow. | **2026-08-15 merge:** the previous 6-screen flow (rating → connections → comment → invite → deep-atmosphere → deep-people) folded into `experience` → `connections` → `wrapup`. The experience screen combines the `RatingFace` mood selector, atmosphere thermometer, connection radar, venue impression, and post-event connection status in one scrollable surface; the wrap-up screen combines attendee impressions (only for mutual picks), improvement suggestions, and optional free-text comment. The invite interstitial and second "均衡反馈" progress language were removed; deep fields are inline and optional, and the server keys `hasDeepFeedback` / XP tier off payload content. Copy uses a purpose hint (`你的观察，会让下一场更对味`) instead of a points pitch. **Rating faces:** 5-point mood selector uses CDN-only `rating-faces` WebP icons via `RatingFace` (`tier='expression'`), with per-asset error fallback to emoji, haptics on selection, and reduced-motion support. **Lovart Batch C (2026-06-04, C5):** `eventFeedbackThanks` ceremony hero (~360×240rpx) sits above the existing `thanksFeedback` Xiaoyue on submission success, reinforcing the "we appreciated this" moment. |
| `pages/pool-registration/index` | Primary destination (subpackage) | Collects pool-specific soft preferences and submits the user into a blind-box event pool. Reads `invitationCode` from URL params and `user.pendingReferralCode` for invite attribution. | Lives in the `pages/pool-registration` subpackage; pool-specific hero backdrops remain bundled at `src/pages/pool-registration/assets/ceremony/`. **「订座」仪式重构 (2026-08-17):** the standard success surface now renders the shared `RegistrationSuccessCeremony` via `PoolRegistrationSuccessCeremony` (analytics/notify/pills/duo slots + `ChemistryMiniGrid`), replacing the legacy `PoolRegistrationSuccess` and `TicketSuccessView`. The confirm modal uses the `ReservationTicket` flat variant. Server disambiguates invitation codes (event-specific `invitations` table first, then permanent `referral_codes`). Self-referral and dedup guards applied. Referral conversions recorded on successful pool registration. **Ceremony redesign (2026-06-14):** `PoolRegistrationHero`, `XiaoyueLetterCard`, and `XiaoyueCoachCard` power the invitation flow; step-completion feedback includes a green check pill, dynamic CTA labels, and Xiaoyue chat reactions (replaced 2026-08-05 by the dedicated mascot section — see Plus 2026-08-05). **Step 3 alignment (2026-06-23):** Step 3 is wrapped in `XiaoyueCoachCard` matching Steps 1/2; the read-only summary card shows only budget + intent (editable fields removed); Step 3 eyebrow is derived from the event-type flow label (`饭局` → “细节”, `酒局` → “氛围”); step wrappers append a `--reduce-motion` class when the user prefers reduced motion; the mascot briefly switches to `celebrate` on the first detail selection (gated by `reduceMotion` and device tier). **Step 3 summary redesign (2026-07-07):** the locked-preferences card now uses a warm cream background with a soft shadow, a purple-accent “已选好的偏好” header, a muted “已锁定” status pill, and icon-led rows (`👑` budget via `JoyJoinIcon` `ui` tier, `🎯` intent via `semantic` tier). Values render as custom read-only pills rather than interactive `Chip` components; empty selections show a dashed-border “未选择” pill. The card exposes `role='group' aria-label='已选偏好（已锁定）'`. Summary derivation is extracted into the tested `buildSummaryItems` helper in `poolRegistrationForm.ts`. Dietary default copy changed from “无限制” to “无特殊要求”. **Terminal-state card parity (2026-06-30):** `PoolRegistrationAlreadyJoined` and `PoolRegistrationSuccess` render `EventSummaryCard` so the post-registration event summary matches the "我的足迹" card design. The card is read-only on confirmation screens (no hover/tap hint/navigation). Terminal states wrap content in `ScrollView` for small-screen reachability; primary CTAs include haptics and analytics (`registration_terminal_state_view`, `registration_terminal_cta_tap`, `registration_terminal_notify_tap`). The notification-enable button shows loading/disabled states and "已开启提醒" on success. **Intent card UI (2026-06-17 / icon-led redesign 2026-08-05):** Reusable `IntentCard` component (`apps/mini-program/src/components/intent/IntentCard.tsx`) with `JoyJoinIcon tier="intent"` (bundled `intent-icons` WebP as single 144×144 files, no `@2x`/`@3x` variants; WeChat downscales). Cards render as **icon-led tiles**: 144rpx hero icon centered, label below, subtitle caption underneath (`min-height` 280rpx). Intent pre-populated from user profile; selection logic delegates to shared `toggleIntentValue()` from `@shared/constants` — **no cap on pool-registration** (default unlimited), and selecting every explicit option auto-collapses the selection to `随缘` (flexible); `essential-data`/`edit-profile` keep their explicit 3-cap. Dimmed state when `随缘` active uses token-based muted colors. `usePreloadIntentIcons` pre-warms bundled icons before the grid renders. Haptics on every toggle; `registration_intent_toggled` analytics event. The 2026-06-17 cap behaviour (cards disabled at 3, completion pill "已经选满 3 个期待，先聚焦这些方向", 「最多选择 3 个期待」 toast) was removed 2026-08-05. Brief card with price pill + archetype cluster + 悦仔 mascot; stagger entrance on step transitions; already-registered guard with mascot + meta rows + events CTA; haptics on `ChoiceCard`/`ChoiceChip`. **Resume notice (2026-08-05):** the payment-detour resume card shows a slim single-line notice 「你的选择已经帮你留好了，确认后继续加入这场局。」 + persistence pills (both handoff codes); paid state kicker 「已确认」. **Flow teaser strip (2026-08-01):** XiaoyueLetterCard Step 0 renders `PoolTeaserStrip` (`src/pages/pool-registration/components/PoolTeaserStrip.tsx`), a hybrid in-letter footer (divider → voice line → 6 flow nodes → trust label) previewing the post-match flow. Gated by `user.features.poolTeaserEnabled` (DB flag `poolTeaserEnabled`, env `POOL_TEASER_ENABLED`, default `false` — ships dark; client fail-closed `=== true`). Emits `pool_teaser_impression` once per mount (whitelisted in `discoverAnalytics.ts` + server `analytics.ts`); replay armed via `useResetOnShow`. Motion: transform/opacity only, `$ease-reveal-rise`, total stagger ≤900ms, `prefers-reduced-motion` + degradation-tier gating. Preloads flow-1..5 + flow-7 via `POOL_REGISTRATION_PRELOADS` (`routePreloadAssets.ts`). |
| `pages/event-coordination/index` | Primary destination | Hosts the post-match event coordination chat and report flow. | Runtime communication surface after matching. |
| `pages/matching-status/index` | Primary destination | Shows waiting, matched, no-match, and cancelled states for a pool registration. | Main waiting and reveal-state hub. Now includes **Unified Connection Reveal** — fuses `chemistryPayoff` group narrative with `connectionPoints` pair evidence into a single card. **Interaction polish (2026-06-02):** haptics on all 10+ buttons (light for secondary, medium for primary CTAs). Celebration `haptics('success')` on match reveal. Spring entrance animations for error/cancelled/no-match/not-found states. Xiaoyue `optOutReassure` mascot in no-match state with bounce-in animation. Reduced-motion suppression on all new animations. **Lovart Batch D (2026-06-04, D4a–e):** `UnifiedRevealCard` carries 5 chemistry-pair hero tiles (240rpx, purple gradient backdrop) keyed by `MATCH_REASON_BADGE_MAP[emoji]` and paired with the existing Batch B `REVEAL_MAP` icon (96rpx). Hero wrap is `aria-hidden`; the screen-reader label lives on the icon. **Event card redesign (2026-07-09):** replaced the generic icon-row event info card with the shared `EventSummaryCard` (gradient shell, status pill, event-type corner vignette, live countdown, match-score footer) so the matching-status event summary matches the "我的足迹" `FootprintOracleCard` design; removed the redundant header status chip; aligned page status labels with the shared event-card vocabulary (`排桌中` / `排桌完成` / `圆满结束`; renamed from 匹配/匹配成功 2026-08-18, see `docs/runbooks/wechat-review-submission.md`). **Tablemate card-deck reskin (2026-08-16):** the matched-state member carousel now deals reusable `TablemateCard` components (squad-unboxing deck front-face recipe: archetype-tinted foil frame, 52/48 art/info split, connection-pill / pair-temperature chip, deal-in entrance with `dealKey` session-memory so swipe-back does not replay the whole hand). Tapping a card opens `TablemateDetailSheet` with full governed copy, rarity dots, interest tags, and a `现场见` CTA; the sheet supports drag-to-dismiss, hero-image fade-in, avatar preview, and `catchMove` scroll containment. Shared pair-chemistry helpers live in `apps/mini-program/src/lib/utils/pairChemistry.ts`. **QA hardening (2026-07-09):** historical match cards use `Taro.redirectTo` so the page reloads correctly; missing `resolvedGroupId` is guarded with a toast and query invalidation before navigation; navigation failures also surface a toast.
| `pages/squad-unboxing/index` | Primary destination | Runs the matched reveal and post-reveal transition into group detail and event follow-up. | **Redesign v2 (2026-06-29):** light warm gradient background; Xiaoyue AI 团魂 bubble with `TypewriterText` reveal (≤3 s) showing the archetype-mix line and group-theme companion; fanned `SquadDeckStage` of collectible `TeammateCard` components with archetype-tinted borders/shadows; tap-to-focus lifts the card and narrates the member intro in the Xiaoyue dock bubble (the inline `TeammateCardDetail` panel was retired 2026-07-14); empty-deck state shows Xiaoyue `actionFailure` mascot; action dock shows `确认出席`, `生成队伍海报`, `查看活动详情`, and `稍后再看`; confirming attendance triggers a Xiaoyue success overlay. **Layout hardening (2026-07-07; ready stage updated 2026-07-24):** fixed stage is now a true fixed-height header (`top: 0`) with state-specific heights — `ready`/`shaking` reserve `clamp(400rpx,46dvh,540rpx)` for the gift box (enlarged from 360×292→400×324rpx), and `revealed` reserves `clamp(500rpx,56dvh,760rpx)` for the fanned deck (updated 2026-07-13 fan revamp); scroll container padding-top matches the current stage height via the root `squad-unboxing--${flowState}` class; removed the `safe-area-top` mixin that was collapsing the scroll offset to 64rpx; removed the redundant `stage-spacer`. The white instruction card and shaking copy card were deleted (2026-07-24) — the ready state is header-only with eyebrow/title/tagline, ribbon restacked below header. Ready-state vertical centering uses auto margins (header + ribbon split free space evenly). **Entrance orchestration polish (2026-07-07):** header, analysis bubble, chapters, inline detail, pair cards, topic chips, and action zone use state-driven CSS transitions with controlled `transitionDelay` stagger; card focus fires `haptics('light')`. **Cascading Hand-Fan revamp + round-3 (2026-07-13):** revealed members render as a two-row cascading hand-fan (≤4 single row, 5+ ceil/floor split; >8 collapse to a `+N` badge) with a slim title bar (`第N组` + chemistry word + `你的桌友来了`); each card is a two-zone collectible — art zone (52%, pure art + 我 badge / +N chip / rotated 最佳拍档 stamp) + info zone (48%, strict 4-row grid: name / accent archetype / grey `age·gender` meta / 1 hook pill — the viewer's strongest connection point when one exists, else the member's top interest as a neutral-outline fallback, 2026-07-16); industry + education surface in the focused-card narration bubble (education clause added 2026-07-16), the face-up aria-label, and — since 2026-07-16 — the focused card's art-zone bottom caption (`本科 · 互联网产品` overlay; covered cards never carry them because the fan overlap leaves only ~126rpx of visible text); cards are 332rpx tall, 190–222rpx wide by member count; geometry is pure `computeFanLayout()` mirrored into SCSS (drift-locked by tests, zero runtime measurement); the 今晚这桌 chapter is a date-led event-brief card (big day numeral, 饭局/酒局 pill, OracleCard corner vignette, inline 复制 chip, theme + vibe + highlights); 团魂 bubble opener is `人到齐了！` (the puzzle metaphor belongs only to matching-status's puzzle prelude). Canonical spec: `AGENTS.md` squad-unboxing section + `docs/deliberations/2026-07-13-squad-unboxing-fan-revamp-locked.md`. **QA runbook:** `docs/runbooks/mini-program-squad-unboxing-smoke.md` — DevTools preview is mandatory for every state to catch overlap / clipping. **Navigation wired (2026-06-28):** `matching-status` routes first-time matched users to `squad-unboxing`; `center-hub` respects the per-group `jj_revealed_${groupId}` storage flag and skips the animation on repeat visits. **Drag-to-reveal ribbon (2026-06-08):** `DragRevealRibbon` replaces the static "揭晓桌友" button with a touch-driven slider that physically lifts the blind-box lid in real time. Drag progress drives lid lift, interior fade, body bounce, aura pulse, and shadow shrink via `transform`/`opacity` only. Snap threshold at 50%; haptic feedback on reveal. Fallback to tap-only for reduced-motion, degradation-tier devices, or when `squadUnboxingDragRevealEnabled` kill switch is off. Analytics: `squad_unboxing_reveal_drag` + `squad_unboxing_reveal_tap`. **Accessibility:** cards are `role="listitem"`, focus is tracked with `aria-live="polite"`, reduced-motion disables entrance/focus transitions, degradation-tier shortens transitions. Analytics: `squad_unboxing_card_focus`, `squad_unboxing_confirm_attendance_tap`, `squad_unboxing_confirm_attendance_success`, `squad_unboxing_confirm_attendance_error`, `squad_unboxing_share_poster_tap`, `squad_unboxing_bubble_reveal_complete`, `squad_unboxing_box_open_milestone`. Server: `POST /api/pool-groups/:groupId/confirm-attendance` returns a clean 200 when attendance is already `confirmed`. **Full-marks hardening (2026-07-08):** inline-detail scroll math centers the detail below the fixed stage so it never overlaps the focused card; ScrollView uses `calc(100vh)` before `100dvh`; share-poster CTA copy is "保存这桌记忆". **Gift-box tap + ribbon anchor (2026-07-09):** the gift box is a tappable button in `ready` state with pressed-state feedback and `aria-label="点击拆开礼盒"`; `DragRevealRibbon` is anchored below the box (not inside the copy card) and uses conditional `catchMove` so drag mode blocks parent scroll while tap-fallback/degradation mode allows vertical scroll. Adds `squad_unboxing_box_tap` analytics. **Tap-to-reveal (2026-07-14, commit `a6a160dc8`):** cards deal face-down and flip up via controller-owned `squadFlipState.ts` (`auto_me` on deal for my card, `tap`, or `reveal_all` via the live-count chip); face-up taps resolve through `resolveCardFocusInteraction` (focus / typewriter fast-forward / dismiss-unfocus emitting `squad_unboxing_card_detail_dismiss`, reinstated in the upstream merge `a6ea57284`); narration lives in the dock bubble (burst > member > tease > soul); the center-card auto-peek and session front holo are retired (a one-time shimmer sweep plays across the face-down backs instead); sibling dimming removed (upstream `13b51fcb6`); the page moved to the `pages/squad-unboxing` subpackage (preloaded from events / center-hub / matching-status). New analytics: `squad_unboxing_card_flip` (method=auto_me/tap/reveal_all), `squad_unboxing_reveal_all_tap`, `squad_unboxing_all_revealed` (single-fire per session) + diagnostics; canonical funnel: `docs/systems/squad-unboxing-analytics-funnels.md`. **盲盒心理 wow pass (2026-07-24, audits at full marks):** cards widened to the 245rpx 3-per-row ceiling (N≤6; N=4 → [2,2]; N=7/8 keep 190rpx), fan scale 0.85, stage clamp 640rpx; every pair-backed card leads with a tier-aware pair-temperature chip + shortened connection pill (covered cards carry the chip only); focused member narrates via a structured 同频分析卡 (verdict → evidence chips → opener) with dignity-floor fallback copy (never concedes "没找到共同点"); 桌型诊断 role-mix chips (deterministic, no LLM) in the bubble footer; 最佳拍档 slow gold flip with heartbeat haptics; card backs chemistry-tier tinted (variable-reward pricing pre-flip); press-and-hold +8° anticipation; flip light trails; peak-end settle breath; 我 card role badge （气氛担当/深度担当/暖心担当） + role narration; flip hold-to-onLoad (never flips into a skeleton); 人→关系→场合 transition line + event-card chemistry foil border; 「这桌的桌卡」 collectible + 750×1100 canvas poster saved to album (`squad_unboxing_table_card_tap/saved/save_failed`, canvas unmounts after save); lit CTA 「确认出席 · 锁定座位」 after all flips (never gated) + return thread. Final audits: ui-layout 100/100, frontend-design 20/20, performance 60/60, user-satisfaction 24/24; 229/229 tests. |
| `pages/pool-group-detail/index` | Primary destination | Shows the matched group detail, members, event details, and AI group analysis. | Read-only matched group surface. Primary CTA opens the gathering room when `gatheringRoomEnabled` is on. **Tablemate deck strip (2026-08-16):** the member summary area now renders a horizontal `TablemateCard` deck (same front-face recipe as matching-status/squad-unboxing) with real overflow detection, scroll-progress dots, a breathing "左滑看更多" hint, and an `aria-label` describing the deck. Tapping a card opens the same `TablemateDetailSheet`. The current-user row is accented on the left; collapsed member rows show a `+N` chip. |
| `pages/gathering-room/index` | Primary destination (subpackage) | Pre-event online anteroom: shows the matched group as V2 pixel avatars with equipped outfits around a shared room scene, plus ephemeral WS presence and attendance confirmation. | Gated by `gatheringRoomEnabled`. Uses `GET /api/pool-groups/:groupId/room-state` and `ROOM_*` WS events. Room art is the delivered composite WebP (`room-composite-v2.webp`, repaired 2026-08-27; v1 retained for rollback) with CSS placeholder fallback. **Polish round 3 (2026-08-27):** floating translucent pill header (shared `SegmentedCountdownClock` + headcount/confirm counts); member sheet swapped to `TablemateDetailSheet` with opt-in `actionSlot` for poke row; universal name plates at every seat (absent = plate only, present = avatar above plate, own plate has 「我」chip); 3/7–8 seat maps; soft terminal state; secondary 看看活动详情 CTA. |
| `pages/icebreaker-session/index` | Primary destination | Runs the live Social Icebreaker session for matched groups at event time. | **PhaseHeroCard PR3 (2026-07-17):** all 9 non-warmup phases migrated to `PhaseHeroCard` shared premium frame with per-phase foil accent; `phaseViews.tsx` now exports hero views exclusively; `phaseAccents.ts` + `_phase-hero-card.scss` added; feature flag `socialIcebreakerPhaseHeroEnabled` removed; session subpackage 40.8→30.3 KiB. Warmup hero card state machine extracted to `warmupViewModels.ts`. `MomentCardView` overhaul with keepsake topic editorial block. Current warmup is 2026-07-16 layout. **Slim host band (2026-07-16):** a single-line mascot band (40rpx expression avatar + toneLine, no background) replaces the previous permanent XiaoyueChatBubble card across all phases; the full bubble is event-driven (everyone-ready celebration, recap). Host tools live in a `⋯` outlined-circle menu that is visible all phases where valid actions exist; tier change is only offered in waiting/warmup. **Warmup 4-band entry (2026-07-16):** welcome band with archetype-woven copy + mode caption, hero card slot (flex:1), chrome-less presence strip, and white action bar. The hero card is a state machine: host picks mood inside the card, the card shows a generating shimmer, then deal-flips to the first topic; deep-chat prompts collapse into a `深聊锦囊 ›` expander; ready CTA morphs from `我准备好了` to a calm `已准备 ✓` state; AIGC labels are a single quiet footer line. Includes `BonusGateOverlay` for the `mini_script` bonus vote gate and server-rendered Moment Card PNG share flow. **Single-test disclosure (2026-07-07; updated 2026-07-08):** single-player test sessions pause at a `TestModeDisclosure` overlay in `warmup` and require the host to explicitly tap **查看总结** (or **重试** on failure) to advance to `recap`; auto-advance is disabled for these sessions. The disclosure is dismissible via an 88rpx close button, includes `min-height: 70vh` + `70dvh` fallback, empty-roster/loading states, a mascot pop-in animation, and a warm "ready" hint when `runBots` is enabled. Server runs deterministic, seeded, LLM-free bot simulation across all multiplayer phases when `runBots === true`. **Lovart Batch C/D (2026-06-04):** `RecapPhaseView` carries the `seeYouNextTime` C6 end-of-session hero (480×320rpx) and the `recapStamp` D5 milestone seal (280rpx) with wax-press animation. Reduced-motion suppresses the stamp entrance. |
| `pages/icebreaker-session/tier-selector/index` | In-event subpage | Host picks the Social Icebreaker session shape before the live session starts. | **Preset-card redesign (2026-06-23):** Replaced the dense 3×3 tier×vibe grid with 3 human-intention preset cards (`轻松破冰`, `深度畅聊` · 推荐, `游戏狂欢`). Each preset maps to a tier+vibe combo. `自定义时长 / 氛围` toggles the original advanced grid for power users. Custom mode (`自由局`) remains as a separate card. Preset/custom cards use CDN-backed Lovart WebP backgrounds (`tier-card-breeze.webp`, `tier-card-glow.webp`, `tier-card-blaze.webp`, `tier-card-custom.webp`) loaded via `cdnAsset()`, consistent with the rest of the Lovart icebreaker asset tree. A11y polish (2026-06-24): `JoyJoinIcon` checkmarks, CSS-drawn chevrons, haptics, `aria-label`/`aria-pressed`/`role="button"` on preset/advanced/grid cells. Analytics: `preset_selected`, `advanced_mode_opened`, `custom_mode_selected`. **情绪价值 + resilience pass (2026-06-25):** warm, community-oriented copy ("大家", host-as-curator framing), Xiaoyue reactions tailored per tier×vibe combo, a preview affirmation line, and idempotent exponential-backoff retry on `POST /api/social-icebreaker/start` with offline detection. |

### Compatibility and legacy alias routes still present in active code

| Page path | Role | Purpose | Notes |
| --- | --- | --- | --- |
| `pages/journey/index` | Compatibility alias | Legacy journey entry that now redirects into the canonical events tab. | Keep labeled as compatibility only. |
| `pages/my-events/index` | Compatibility alias | Older my-events entry that also redirects into the canonical events tab. | Keep labeled as compatibility only. |

**双人成行 Duo registration (2026-08-07):** Pool registration supports a duo mode gated by `features.duoRegistrationEnabled`. Step 0 shows `PoolRegistrationDuoCard` with a segmented 1人／2人 toggle; the info icon opens `DuoInfoSheet` to explain the rules. When the user enters via a duo invitation, `PoolRegistrationDuoBanner` surfaces the invitee context. The invitation share path is `/pages/pool-registration/index?id={poolId}&invitationCode={code}&duo=1`. Bound duos are materialized at registration time when both sides hold registrations in the pool.

## 5. Mini-program user flows

| Journey | Main mini-program path | Canonical notes |
| --- | --- | --- |
| New user onboarding | `pages/index/index` -> `pages/onboarding/personality-test/index` -> `results` (inline login) -> `essential-data` -> `extended-data` -> `profile-review` -> `pages/discover/index` | Product and step ownership live in [`../systems/onboarding-flow.md`](../systems/onboarding-flow.md). |
| Returning login | `pages/index/index` (guest secondary CTA or `?auth=logout\|expired` loggedOut state) -> WeChat login -> `/api/auth/user` -> server-owned `nextStep` | Mini-program-native login only; no browser OAuth redirect. |
| Discovery and pool registration | `pages/discover/index` -> optional `pages/event-detail/index` -> `pages/pool-registration/index` | Pool registration owns soft-preference capture before matching. |
| City unlock (no city interest) | `pages/discover/index` -> `CityUnlockFeedCard` -> `CityPickerSheet` -> `POST /api/cities/interest` -> `pages/city-unlock/index` | **City Unlock v0.1** (2026-05-19, updated 2026-06-23). Entry via CityUnlockFeedCard (bottom of pool list) or profile. `CityUnlockBanner` removed 2026-06-10 and deleted from source — GPS auto-filter + feed card are sole entry points. City picker and area filter (`LocationFilterDrawer`) share a unified `PickerShell` + `SelectableTile` design system: consistent Xiaoyue mascot header, filled-primary selected state with white checkmark, 8rpx spacing rhythm, `transform`-only motion, reduced-motion gating, and safe-area handling. City picker: search + pinyin-aware hot-city grid + full city list + scroll-to-selected + inline Xiaoyue celebration + inline error/offline retry. Area drawer: cluster sections + 2-column district grid + heat badges + pending "待解锁" badge. 88rpx touch targets throughout, `role=dialog` + `aria-labels`. Tracked via 9 `city_picker_*` and 3 `filter_*` analytics events. Offline detection before API call. Progress page shows threshold countdown, share to invite, and other-city interest. |
| 街头盲盒｜Flash NPC | `pages/discover/index` -> `pages/alang/event/index` -> `pages/alang/search/index` -> `pages/alang/dialogue/index` -> `pages/alang/result/index` -> `pages/alang/archive/index` | 正式链路由服务端 readiness/发布季控制；`alangEnabled` 仅控制旧阿浪原型入口。Discover 静态入口不预取、不申请定位。搜索不显示隐藏坐标/路线，10m 服务端到达后进入 dialogue。结果卡先于归档，归档仪式 24s 内可跳过。复测仅在 strict non-production single-test。 |
| 私人连续故事 | `pages/profile/index` -> `pages/profile-linked/personal-story/index` | `personalStoryEnabled=true` 才开放入口和 API；用户点更新后异步追加所有尚未成章的真实经历，一次一章，不展示活动流水账或分享入口。开关已开但 provider 暂不可用时，旧章仍可读且更新失败不删历史；开关关闭时不访问故事表。 |
| 我的形象 | `pages/profile/index` -> `pages/profile-linked/my-image/index` | `profilePixelAvatarEnabled` 控制形象/衣橱；`equipmentRewardsEnabled` 独立控制抽取和碎片商店。穿脱必须显式保存，奖励不接现金。 |
| Payment and verification | Free registration: `pages/pool-registration/index` -> `pages/blind-box-payment/index` -> `pages/payment-verification/index` -> back to pool registration or onward to profile/events. Paid event registration: `pages/event-detail/index` or `pages/discover/index` -> `pages/event-ticket-payment/index` -> `RegistrationSuccessCeremony` success surface -> events/profile. | Cross-platform coordination rules live in [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md). **「订座」仪式 (2026-08-17):** both paid and free registration success moments converge on the shared `RegistrationSuccessCeremony` + `ReservationTicket` before routing onward. |
| Matching and reveal | `pages/matching-status/index` -> `pages/squad-unboxing/index` -> `pages/pool-group-detail/index` -> `pages/gathering-room/index` (when `gatheringRoomEnabled`) -> `pages/event-coordination/index` or `pages/events/index` | Query keys and invalidation rules live in [`./mini-program-data-fetching.md`](./mini-program-data-fetching.md). Matching-status now runs the **Unified Connection Reveal** — `composeUnifiedReveal()` fuses group chemistry with pair connection points into a single `UnifiedRevealCard`. **2026-08-16 reskin:** both `matching-status` (matched carousel) and `pool-group-detail` (deck strip) render the same reusable `TablemateCard` front-face component; member detail is surfaced in `TablemateDetailSheet`. Gathering-room entry replaces the old `开始破冰` primary CTA on `pool-group-detail` and is also reached after confirming attendance in `squad-unboxing`. |
| In-event icebreaker | `pages/icebreaker-session/index` | Active in-event social flow; server-owned session state is described in [`../ai/AI_FEATURE_INVENTORY.md`](../ai/AI_FEATURE_INVENTORY.md) and [`../architecture/current-state.md`](../architecture/current-state.md). |
| Ongoing account, rewards, invite, and relationship use | `pages/profile/index`, `pages/events/index`, `pages/connections/index`, `pages/profile-linked/rewards/index`, `pages/profile-linked/invite/index`, `pages/profile-linked/edit-profile/index` | These are the steady-state anchors after onboarding and matching. |

## 6. AI features on the mini-program

Mini-program-reachable AI features are inventoried canonically in [`../ai/AI_FEATURE_INVENTORY.md`](../ai/AI_FEATURE_INVENTORY.md). The live mini-program surfaces currently depend on the following feature families:

| Feature family | Mini-program entry surfaces | What matters here |
| --- | --- | --- |
| Onboarding profile tagline | `pages/onboarding/profile-review/index` | Presentation-only AI line fetched during profile review; it does not change onboarding state ownership. |
| Match group analysis | `pages/matching-status/index`, `pages/squad-unboxing/index`, `pages/pool-group-detail/index` | Shared `GroupAnalysisResponse` rendered on matched-state surfaces with cache and fallback behavior. Matching-status now additionally runs `composeUnifiedReveal()` to fuse `chemistryPayoff` with `connectionPointsWithRarity` into a single presentational card. |
| Event theme reveal | Matching-status and group-detail surfaces after matching | Async theme/title generation can enrich the reveal and later group detail state. |
| Social Icebreaker generation | `pages/icebreaker-session/index` | AI-backed warmup topics, micro-challenges, lie-detective content, personality-dice content, and recap summary. |
| Semantic profile embeddings | Onboarding, profile, and interest-update writes | Backend-only enrichment triggered by user changes; not shown as a direct UI widget. |

Practical rule: AI is allowed to enrich copy and in-event facilitation, but the mini-program should still degrade gracefully. Current fallback behavior is documented in [`../ai/AI_FEATURE_INVENTORY.md`](../ai/AI_FEATURE_INVENTORY.md) and validated by [`../runbooks/mini-program-ai-smoke.md`](../runbooks/mini-program-ai-smoke.md).

## 7. Shared contracts and server domain boundaries

The mini-program does not own the entire feature stack. It owns the Taro runtime layer and consumes shared contracts plus server-owned business decisions.

| Ownership layer | Primary files | What it should own |
| --- | --- | --- |
| Mini-program runtime | [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts), [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts), [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts), [`../apps/mini-program/src/pages/`](../apps/mini-program/src/pages/) | Taro page structure, route registration, local storage and pending-order recovery, WeChat runtime wiring, mini-program query keys, and custom tab bar behavior. Includes `AutoLoginBridge` (returning-user silent auth) in `app.ts`. |
| Shared contracts | [`../packages/shared/src/README.md`](../packages/shared/src/README.md), [`../packages/shared/src/api.ts`](../packages/shared/src/api.ts), [`../packages/shared/src/schema.ts`](../packages/shared/src/schema.ts) | DTOs, onboarding mapping helpers, payment and group-analysis response shapes, legal copy, and canonical schema names that multiple runtimes must share. |
| Server domains | [`../apps/server/src/README.md`](../apps/server/src/README.md) | `nextStep` calculation, event-pool registration and matching, group analysis generation, payment creation and verification, notifications, connections, and Social Icebreaker session authority. |

Important boundary rules:

- Treat `/api/auth/user` as the authority for authenticated user state and onboarding progression.
- Treat `packages/shared` as the contract layer when both mini-program and another runtime must agree on payload shape or schema naming.
- Treat auth, API wrapper semantics, payment mechanics, pricing, and entitlement changes as **coordination-sensitive**. Use [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md) before deciding a change is mini-program-only.
- The mini-program is the strongest current reference for the live payment flow, but shared payment behavior is still a `BOTH_REQUIRED` review surface when business rules move.

## 8. Data model quick reference

This section names the core entities the mini-program relies on without duplicating full schema definitions. The canonical source remains [`../packages/shared/src/schema.ts`](../packages/shared/src/schema.ts).

| Entity | Canonical table | Why the mini-program cares |
| --- | --- | --- |
| User identity and onboarding state | `users` | Holds profile fields, archetype, onboarding completion flags, `nextStep` inputs, and entitlement-adjacent user state returned by `/api/auth/user`. |
| Interest selections and heat | `user_interests` | Stores the structured interest carousel output used by onboarding, edit profile, and later matching/personalization. |
| Personality test state | `assessment_sessions` | Stores pre-signup and post-signup assessment progress plus final archetype results. |
| Discoverable pools | `event_pools` | Defines the live blind-box pools shown on discover, including timing, location, constraints, and pool-level status. |
| User pool registrations | `event_pool_registrations` | Stores per-user registration preferences, match status, and assigned group linkage. |
| Matched groups | `event_pool_groups` | Stores group-level reveal data, member counts, theme fields, and group-analysis caches used after matching. |
| Post-match event records | `blind_box_events` | Represents the user-facing blind-box event state after matching, including progress and matched attendee data. |
| Pre-event attendance state | `blind_box_pre_attendance` | Stores user attendance responses and admin attendance overrides before the event happens. |
| Entitlements and payment records | `subscriptions`, `payments` | Back the payment, entitlement, verification, and refund state shown in payment and profile flows. |
| City unlock tracking | `user_city_interests`, `city_unlock_progress` | **City Unlock v0.1** (2026-05-19). `user_city_interests` stores per-user city selections (source-tracked). `city_unlock_progress` tracks per-city interested count, threshold, and status machine (`collecting` → `researching` → `launching` → `live`). |

## 9. Admin flows affecting the mini-program

Endpoint and role coverage live canonically in [`../admin/admin-rbac-matrix.md`](../admin/admin-rbac-matrix.md). Some sensitive writes are also recorded through [`../apps/server/src/lib/adminAuditLogger.ts`](../apps/server/src/lib/adminAuditLogger.ts) where the route has been instrumented, so verify audit coverage per endpoint instead of assuming every admin mutation already logs through that helper. The table below focuses only on the admin actions that mini-program users can feel directly.

| Admin action | Endpoint or authority | Mini-program effect |
| --- | --- | --- |
| Create or update event pools | [`../admin/admin-rbac-matrix.md`](../admin/admin-rbac-matrix.md) (`/api/admin/event-pools*`) | Changes what appears in discover, what users can register for, and the timing/location/constraint copy shown in event detail and pool registration. |
| Run matching for a pool | [`../apps/server/src/routes.ts`](../apps/server/src/routes.ts) (`POST /api/admin/event-pools/:id/match`) | Creates matched groups and unlocks or changes what registered users see in matching status, reveal, and group-detail flows. |
| Override blind-box attendance | [`../apps/server/src/routes.ts`](../apps/server/src/routes.ts) (`PATCH /api/admin/blind-box-events/:eventId/attendees/:userId/attendance`) | Changes the stored pre-event attendance state that matched-event flows read later. |
| Send or broadcast admin notifications | [`../apps/server/src/routes.ts`](../apps/server/src/routes.ts) (`POST /api/admin/notifications/broadcast`, `POST /api/admin/notifications/send`) | Changes the notification counts and user-facing reminder or announcement traffic that mini-program tabs mark as read. |
| View city unlock report | [`../apps/server/src/routes/domains/cityUnlock.ts`](../apps/server/src/routes/domains/cityUnlock.ts) (`GET /api/admin/cities/unlock-report`) | **City Unlock v0.1** (2026-05-19). Admin view of per-city interested counts, status, and recent signups. Ops receives WeCom notification when a city crosses threshold (50) and transitions to `researching`. |
| Ban or unban a user | [`../apps/server/src/routes.ts`](../apps/server/src/routes.ts) (`PATCH /api/admin/users/:id/ban`, `PATCH /api/admin/users/:id/unban`) | Banned users lose normal authenticated access until restored; unban restores that path. |
| Initiate a refund | [`../apps/server/src/routes/domains/payments.ts`](../apps/server/src/routes/domains/payments.ts) (`POST /api/admin/payments/:paymentId/refund`) | Can reverse a payment-backed entitlement or pack outcome that later appears in profile and payment recovery flows. |

## 10. Platform coordination boundary

Use [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md) whenever the mini-program change might alter shared business behavior rather than only Taro runtime behavior.

| Scope | Typical examples | Review expectation |
| --- | --- | --- |
| `MINI_PROGRAM_ONLY` | Route registration, Taro page composition, WXSS-safe presentation, custom tab bar state, local pending-order storage, mini-program query keys | Mini-program implementation review only. |
| `BOTH_REQUIRED` | Auth/session bootstrap, `/api/auth/user` semantics, payment flow behavior, pricing or entitlement rules, request wrapper semantics, shared DTO changes | Review the matching web or shared surface before finalizing the change. |
| Shared-contract-first | Schema names, API DTOs, onboarding helpers, legal copy | Update `packages/shared` first and then confirm both consuming clients still align. |

Default rule: if a mini-program task touches auth, payment, pricing, or another shared contract and the ownership is unclear, treat it as `BOTH_REQUIRED` until [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md) says otherwise.

## 11. Environment and build configuration

| Concern | Source of truth | Current note |
| --- | --- | --- |
| Workspace commands | [`../apps/mini-program/README.md`](../apps/mini-program/README.md) | Use `npm run dev:weapp --workspace=mini-program` and `npm run build:weapp --workspace=mini-program` for the live mini-program workspace. |
| CI build target | [`.github/workflows/taro-weapp-build.yml`](../../.github/workflows/taro-weapp-build.yml) | **2026-07-20:** automatic uploads target `https://staging.joyjoinapp.com` and run only after `Deploy Staging` succeeds for the same commit. Production builds require an explicit `workflow_dispatch` `api_target=production` selection. Local dev still defaults to `http://localhost:5001`. |
| Page loading and package split | [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts), [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) | The app uses `lazyCodeLoading: 'requiredComponents'`, a main package, and preloaded subpackages (onboarding, pool-registration, matching-status, icebreaker-session). |
| Runtime API base URL | [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts) | `TARO_APP_API_BASE_URL` controls the runtime API origin; local default is `http://localhost:5001`. |
| Launch-critical server env | [`../product/LAUNCH_CONFIG.md`](../product/LAUNCH_CONFIG.md) | `DATABASE_URL`, `SESSION_SECRET`, `WECHAT_APPID`, and `WECHAT_SECRET` are required; `PAYMENTS_ENABLED` gates live payment availability. `MOCK_PAYMENTS` (default `false`) enables dev-only instantly-paid payment mode for testing without real charges. |
| WeChat Pay enablement | [`../product/LAUNCH_CONFIG.md`](../product/LAUNCH_CONFIG.md) | When payments are enabled, the WeChat Pay variables must also be configured and stay consistent with the mini-program app identity. |
| Tab bar and shell runtime | [`../apps/mini-program/README.md`](../apps/mini-program/README.md), [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts) | The active runtime uses a native custom tab bar with `tabBar.custom: true`. |
| Asset delivery | [`../apps/mini-program/README.md`](../apps/mini-program/README.md), [`../apps/mini-program/scripts/cdn-asset-manifest.json`](../apps/mini-program/scripts/cdn-asset-manifest.json) | CDN-first via `cdnAsset()` with curated local bundles via `localAsset()` for critical first-impression assets (tab icons, logo, fonts, landing phase icons, icon tiers shipped as single bare `.webp` files with no `@2x`/`@3x` variants, empty states, auction icons, Xiaoyue loading/welcome, **Xiaoyue mascot sprite-sheet fallback** — only 6 core states bundled locally, remaining 14 CDN-primary). Production builds guarantee `TARO_APP_CDN_BASE_URL` via `config/index.ts` default + CI fallback (still overridable in `.env.local`). Two-tier font loading: minimal Alimama subset (66KB) bundled + full font (621KB) from CDN. Route-based preloading via `routePreloadAssets.ts`. App-launch onboarding preloading via `preloadOnboardingAssets` (`apps/mini-program/src/lib/utils/onboardingPreload.ts`) warms critical intro/test/intent/mascot assets in staggered tiers, skipping on 2G/offline and deferring heavy mascot sprites on low-end devices. Interest taxonomy v2.0 illustrations (`images/interests/*.webp`) are CDN-only; canonical `imageUrl` lives in `packages/shared/src/interests.ts`. The canonical archetype asset registry lives in `apps/mini-program/src/lib/utils/archetypeAssets.ts`; full-size archetype images are CDN-only and only the primary result image is preloaded on test completion/results mount. Icebreaker phase emblems are preloaded on session entry (`ICEBREAKER_PHASE_EMBLEM_ASSETS`); phase toasts use `JoyJoinIcon tier='phase'` resolved via CDN. Icebreaker card backgrounds use `<ChallengeCardBgImage>` (WeChat-safe `<Image>` component) instead of CSS `background-image`. Package size validator: `npm run check:package-size` (measures zip-compressed size against 2MB WeChat limit, current ~1.88MB zip as of 2026-06-18; **1.82 MB zip as of 2026-07-06 after Phase 1 asset/CDN migration**). Main-package recovery came from bundling only 6 core Xiaoyue mascot sprite states locally and stripping `@3x` variants from bundled status/semantic/ui icon tiers. **Discover hero banner** was bundled locally (`assets/promo-local/`) through 2026-06-18; **2026-07-06:** moved CDN-only to keep the main package under the 2 MB ceiling. **Phase 1 migration summary (2026-07-06):** eliminated puzzle source-to-dist leakage (`_contact-sheet.png` + PNG masters moved to `assets-source/lovart/puzzle/`), removed the pool-registration hero main-package duplicate, moved `xiaoyue-coach-guide.webp` to CDN, moved the Discover hero banner to CDN-only, aligned CDN base URL to `https://joyjoinapp.com/static`, and registered newly CDN-only assets in `cdn-asset-manifest.json`. Asset validators: `npm run validate:assets` (orphan references) and `npm run validate:icon-transparency` (opaque bundled icons on variable backgrounds). `build:weapp` runs both. **Lovart Batches C + D:** originally Path B local-bundle (2026-06-04), moved to CDN on 2026-06-16 to make room for the mascot sprite-sheet fallback + bundled rating faces; registries `ceremonyHeroes.ts` + `milestoneBadges.ts` now use `cdnAsset()` and the assets are declared in `cdn-asset-manifest.json`. **CDN cleanup (2026-06-17):** removed unused `chemistry-badges`, `ui`, legacy `archetypes`, `matching-heroes`, `pages/onboarding`, and `static/assets` from CDN; added missing `reaction`/`reveal`/`achievement` icons plus archetype head/PNG fallbacks. The manifest is the source of truth; run `npm run upload:cdn-assets` after modifying it. |

## 12. QA and maintenance

- Run `npm run typecheck -w mini-program` and `npm run build:weapp -w mini-program` before treating a mini-program change as code-complete.
- Use [`../runbooks/mini-program-ai-smoke.md`](../runbooks/mini-program-ai-smoke.md) when a change touches profile tagline, group analysis, event-theme reveal, or Social Icebreaker AI.
- Use [`../runbooks/mini-program-events-tab-smoke.md`](../runbooks/mini-program-events-tab-smoke.md) when tab-shell or compatibility-route behavior changes.
- Use [`../runbooks/mini-program-tab-bar-smoke.md`](../runbooks/mini-program-tab-bar-smoke.md) when the native custom tab bar, `TAB_BAR_PAGE_PATHS`, or tab-page visibility changes.
- Keep query keys and invalidation notes aligned with [`./mini-program-data-fetching.md`](./mini-program-data-fetching.md), especially for waiting, reveal, and group-detail flows.
- For visual or interaction changes, follow the mini-program quality bar in [`../apps/mini-program/README.md`](../apps/mini-program/README.md) and the linked frontend excellence guidance; this document is not a substitute for WeChat DevTools verification.

## 13. Docs-sync maintenance rules

Update only the sections affected by the source change. Do not rewrite the whole document when one source row changes.

| If this source changes | Re-check sections in this doc | What to confirm |
| --- | --- | --- |
| [`../PRODUCT_REQUIREMENTS.md`](../PRODUCT_REQUIREMENTS.md) | Header, 2, 5, 10 | Launch-primary status, nav terminology, entitlement wording, and journey language still match product canon. |
| [`../apps/mini-program/README.md`](../apps/mini-program/README.md) | Header, 3, 11, 12 | Workspace commands, package strategy, custom tab bar ownership, and launch-primary guidance still match runtime docs. |
| [`../systems/onboarding-flow.md`](../systems/onboarding-flow.md) | 2, 4, 5 | Onboarding routes, step order, and server-owned `nextStep` descriptions still match the active flow. |
| [`../reference/PLATFORM_COORDINATION.md`](../reference/PLATFORM_COORDINATION.md) | 5, 7, 10, 11 | Auth, API, payment, and sibling-platform review rules are still described accurately. |
| [`./mini-program-data-fetching.md`](./mini-program-data-fetching.md) | 5, 7, 12 | Query-key, invalidation, and waiting/reveal references still match live mini-program data flow. |
| [`../architecture/current-state.md`](../architecture/current-state.md) | Header, 3, 7 | Ownership chain and launch-primary architecture notes still align with the current codebase. |
| [`../ai/AI_FEATURE_INVENTORY.md`](../ai/AI_FEATURE_INVENTORY.md) | 6, 12 | The AI surface list, fallback claims, and smoke references are still current. |
| [`../admin/admin-rbac-matrix.md`](../admin/admin-rbac-matrix.md) | 9 | The admin-impact table still maps to the right endpoints and role assumptions. |
| [`../apps/server/src/README.md`](../apps/server/src/README.md) | 7, 9 | Server domain ownership and admin or payment boundaries are still accurate. |
| [`../packages/shared/src/README.md`](../packages/shared/src/README.md) and [`../packages/shared/src/schema.ts`](../packages/shared/src/schema.ts) | 7, 8 | Shared contract ownership and the named data entities still match the canonical schema. |
| [`../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts`](../apps/mini-program/src/lib/onboarding/onboardingRoutes.ts) and [`../apps/mini-program/src/app.config.ts`](../apps/mini-program/src/app.config.ts) | 3, 4, 11 | Page inventory, main-package vs subpackage split (onboarding, pool-registration, matching-status, icebreaker-session), preload rules, and tab anchors still match the live registry. |
| [`../apps/mini-program/src/lib/api/api.ts`](../apps/mini-program/src/lib/api/api.ts) | 5, 7, 10, 11 | Auth bootstrap, API base URL, payment verification behavior, and coordination-sensitive notes still match implementation. |
| [`../runbooks/mini-program-ai-smoke.md`](../runbooks/mini-program-ai-smoke.md), [`../runbooks/mini-program-events-tab-smoke.md`](../runbooks/mini-program-events-tab-smoke.md), and [`../runbooks/mini-program-tab-bar-smoke.md`](../runbooks/mini-program-tab-bar-smoke.md) | 12 | The referenced QA commands and smoke expectations still cover the current routes and tab-bar visibility behavior. |
| [`../product/LAUNCH_CONFIG.md`](../product/LAUNCH_CONFIG.md) | 11, 12 | Environment variable requirements and payment enablement notes are still accurate. |
