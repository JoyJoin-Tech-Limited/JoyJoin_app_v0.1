# T0 一页确认清单（运营 / 产品签核）

- **日期：** 2026-09-16　**细节依据：** `docs/design/budget-tier-t0-venue-truth-worksheet-20260916.md`（516 行，本页只做确认用）
- **为什么需要**：**6/6 场地 `onboarding_status='draft'`**，而派场要求 `='active'` → 派场候选集**恒为空**。且唯一餐厅只有 T馆 `200-300` → **饭局 4 个档位里 3 个无场地**。
- **本页签完 = M0 通过 = T1/T2 解锁。** 在此之前不改任何代码。

---

## A. 决策确认（6 项）

> 「建议」是 PM 立场，**不是决定**。请在「决定」栏写 ✅ 同意 / ✏️ 修改。

| # | 一行问题 | 建议 | 归属 | 决定 |
|---|---|---|---|---|
| **B2** | 弥所 / Bruma / Max 真实**人均**价？ | **2026-09-16 运营决议**：三家按 `≤150 人均` 假设 → `drinks_80_150`。**这不是核实真值**，标 `ASSUMED_PENDING_VERIFICATION`，上线后须用真实菜单/人均复核 | 运营 | ✅ 已决议（假设） |
| **B3** | 盲盒 `100-200`（横跨两档）如何映射？ | **不得静默映射**。① 写入层 fail-closed（要求重选）**长期** ＋ ② 历史行保守归 `dining_150_below`（不诱导超支）**仅作 grandfathered** | 产品 | ☐ |
| **B4** | 确认「最多跨一档」降级政策？（代价：`150以下` 在签下廉价餐厅前仍 `venue_tbd`） | **确认** — 跨两档交人工是诚实修复，防「降级变抬价」 | 产品 | ☐ |
| **B5** | 档位 `min(1)` 必填 + `max(1)` 单选？ | **两者都确认**（必填配 `BUDGET_TIER_REQUIRED`；单选匹配现有注册表单） | 产品 | ☐ |
| **B6** | 下发集合挂哪个端点？ | **`GET /api/event-pools/:id`**（注册页需该池 `eventType`）；按 `城市 × 局型` 缓存 | 后端 | ☐ |
| **B7** | `event_pools.event_type` 是否含英文值或第三值 `其他`？ | **M0 前必须验证**（见 C）。若存在 → 加枚举 + 派场前归一化。**`其他` 现被静默当饭局** | 后端/产品 | ☐ |

## A2. 技术裁定（2 项）

| # | 问题 | 建议 | 归属 | 决定 |
|---|---|---|---|---|
| **B-NEW-1** | Batch & Co `20:00–02:00` 档期**永不匹配**（字典序谓词）→ 唯一酒局场地的部分档期永久失效 | **(a) 重写种子为同日可匹配窗口**（更简单、可测）；(b) 谓词加跨零点（改核心逻辑）。**不建议**保留现状 | 运营+后端 | ✅ **(a) 已决议**（2026-09-16） |
| **B8** | `parseEventDate` 取 **UTC 墙钟**当本地时间——写入端若存真 UTC 则档期/派场时段整体错位 | **先验证** `event_pools.date_time` 的实际存储约定，再跑档期覆盖校验 | 后端 | ☐ |

---

## B. 场地真值（6 行，填 ⬜）

> 仓库值仅作参考（`[repo]`），**每格须运营确认**。⛔ = 仓库值已知矛盾，**必须给真值**。

| 场地 | 类型 | 现档位 `[repo]` | 真实价位 | 建议档位 id | 单位 | 合同起/止 | 确认 |
|---|---|---|---|---|---|---|---|
| 弥所 | bar | `150-200` ⛔ | 人均 **≤150（假设）** | `drinks_80_150` | per_person | `____`/`____` | ⚠️ 假设待复核 |
| T馆·艺术餐厅 | restaurant | `200-300` | 人均 `______` | `dining_200_300` | per_person | `____`/`____` | ☐ |
| Bruma | bar | `150-200` ⛔ | 人均 **≤150（假设）** | `drinks_80_150` | per_person | `____`/`____` | ⚠️ 假设待复核 |
| Max Shenzhen | bar | `300-500` ⛔ | 人均 **≤150（假设·大幅下调）** | `drinks_80_150` | per_person | `____`/`____` | ⚠️ 假设待复核 |
| Delete Bar 大喇叭精酿 | bar | `80-150` | 人均 `______` | `drinks_80_150` | per_person | `____`/`____` | ☐ |
| Batch & Co | bar | `80-150` | 人均 `______` | `drinks_80_150` | per_person | `____`/`____` | ☐ |

> ⚠️ **三家为运营假设**（`ASSUMED_PENDING_VERIFICATION`）：`≤150 人均` → 统一落 `drinks_80_150`。Max 由仓库 `300-500` 降至 ≤150 属**大幅下调**，尤其需要上线后复核。真实值若 >150，则启用预留档 `drinks_150_200`（不改 id、不重排 order）。
> **单位已统一为 `per_person`**（Q3 修订）：酒局不再按"每杯"，因此**不再需要人均→每杯换算**——这正是原缺陷的成因。

**另需确认：** ① T馆之后是否还会新增餐厅？（决定 `150以下`/`150-200` 是否长期无解） ② Batch & Co 的 `area`/`district_id` 是否影响城区过滤（`venueAssignmentService.ts:342`）。

---

## C. 先跑这 3 条只读 SQL（验证，不改数据）

```sql
-- C1 · B7：event_type 实际值域（staging + prod 各跑一次）
SELECT event_type, count(*) FROM event_pools GROUP BY 1 ORDER BY 2 DESC;
SELECT DISTINCT event_type FROM event_pools WHERE event_type NOT IN ('饭局','酒局');

-- C2 · B8：date_time 存储约定（取 1 条已知活动，核对是否等于本地墙钟）
SELECT id, date_time, event_type FROM event_pools ORDER BY created_at DESC LIMIT 5;

-- C3 · 档期覆盖：按官方（worksheet §5.2 Q5）对每个测试池 dateTime 校验
--     详见 docs/design/budget-tier-t0-venue-truth-worksheet-20260916.md §5.2
```

**若 C1 出现英文值或 `其他`，或 C2 显示 UTC 错位** → **先在「决定」栏记录，再继续**（这两项会改变覆盖结论）。

---

## D. 零供给白名单（运营勾选接受的缺口）

> 未列入白名单的档位 = 必须补齐场地才能发布。

| 城市 × 局型 | 档位 | 现状 | 处理 |
|---|---|---|---|
| 深圳 × 饭局 | `dining_150_below` | 0 场地 | ☐ 招募 ☐ 列入白名单 ☐ 调整档位 |
| 深圳 × 饭局 | `dining_150_200` | 0 场地 | ☐ 招募 ☐ 列入白名单 ☐ 调整档位 |
| 深圳 × 饭局 | `dining_300_500` | 0 场地（新发现） | ☐ 招募 ☐ 列入白名单 ☐ 调整档位 |
| 深圳 × 酒局 | `drinks_80_below` | 0 场地 | ☐ 招募 ☐ 列入白名单 ☐ 调整档位 |
| 香港（两局型） | 全 6 档 | 0 场地 | ☐ 招募 ☐ 列入白名单 ☐ 关闭该城市 |

---

## E. 签核

M0 退出条件（全部满足才通过）：

- [ ] A 表 6 项决策已填
- [ ] A2 表 2 项技术裁定已填
- [ ] B 表 6 行场地真值已填并逐行确认
- [ ] C 三条 SQL 已在 **staging + prod** 各执行并记录结果
- [ ] D 白名单已勾选（未列白名单的零供给档位 = 阻塞发布）
- [ ] 档位 id 集合已冻结（含预留 `drinks_150_200`）
- [ ] 弥所缺陷已关闭

| 角色 | 姓名 | 签名 | 日期 |
|---|---|---|---|
| 运营 | | | |
| 产品 | | | |
| 后端（技术裁定） | | | |

> 签核完成后 → 解锁 `T1`（种子 `active` + 重打标 + 夜间窗口修复）与 `T2`（注册表 + 归一化器）。
