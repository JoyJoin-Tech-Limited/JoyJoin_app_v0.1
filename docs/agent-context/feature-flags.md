# Feature Flags — Agent Context

> Extracted from AGENTS.md §6 (2026-07-31). Load when adding/changing/auditing a feature flag. Skill: `feature-flags-launch-config`.

**Feature Flags (DB-backed, 2026-07-15):** `apps/server/src/lib/featureFlags.ts` is the canonical resolver. DB row is source of truth; env var is fallback. 5s LRU cache (disabled in test env). Kill switches exposed in auth response `features`: `restartOnboarding`, `smartProfession`, `onboardingForceSkip`, `matchingLiveReveal`, `socialIcebreakerClientForceEnd`, `runPlanTemplatesEnabled`, `promoBannerEnabled`, `personalityShareEnabled` (gates share poster generation), `personalitySlotAnimationEnabled` (gates slot machine reveal animation), `personalityTestEchoEnabled` (gates answer-echo loading state, default `true`), `squadUnboxingDragRevealEnabled` (default `true`), `socialSquadComposedHeroEnabled` (default `false`), `profileRedesignEnabled` (Profile parent rollback, default `true`), `profilePixelAvatarEnabled` (Profile-only pixel avatar/My Image, default `false`), `equipmentRewardsEnabled` (inventory/rewards, default `false`), `personalStoryEnabled` (entire private-story surface and updates, default `false`), `socialIcebreakerCustomModeEnabled` (default `true`), `oracleCardCornerStatEnabled` (default `true`), `personaSnapshotEnabled` (default `true`), `matchingPuzzlePreludeEnabled` (default `false`), `matchingOperatorReviewEnabled` (default `true`), `matchNeverMeetSentinel` (Magnetism Engine Phase 0 / W2 — gates the `match_history` `wouldMeetAgain === false` -1 hard-skip sentinel in pair scoring; env `MATCH_NEVER_MEET_SENTINEL`, default `false`, policy-pending per `docs/systems/MAGNETISM_ENGINE.md` §7; the +5 re-match boost is unconditional), `matchChemistryCalibrationEnabled` (Magnetism Engine Phase 0 / W3 — gates the chemistry-calibration READ path in `matchEventPool`; the stats writer is live via match_history derivation but calibrated deltas are Phase-3 scope; env `MATCH_CHEMISTRY_CALIBRATION_ENABLED`, default `false`), `magnetismGroupRulesEnabled` (惊艳开局包 P1 — four group-composition rules: R1 no-isolate ≥60 strong tie, R2 energizer ≥75 with pool exemption, R3 topic anchor with cold-start skip, R4 explore dispersion −8 nudge; R1–R3 enforced at the formation commit gate **and re-checked in all three §4.5 redistribution phases** via `magnetismRulesSatisfiedFor`; env `MAGNETISM_GROUP_RULES_ENABLED`, default `false`), `magnetismWeightProfileV2Enabled` (惊艳开局包 P2 — theory-ranked weight rebalance chemistry 28→20 / interest 28→32 / socialAffinity 20→23 / language 4→5; env `MAGNETISM_WEIGHT_PROFILE_V2_ENABLED`, default `false`), `poolTeaserEnabled` (gates the pre-registration flow teaser strip `PoolTeaserStrip` inside the XiaoyueLetterCard on pool-registration, env `POOL_TEASER_ENABLED`, default `false`), `contentModerationMsgSecCheckEnabled` (Tier-1 semantic moderation via WeChat msgSecCheck on top of the deterministic keyword filter; env `CONTENT_MODERATION_MSGSECCHECK_ENABLED`, **default `true`**; latency bounded by `CONTENT_MODERATION_TIER1_BUDGET_MS` = 250ms, fails open — late risky verdicts are enforced in the background), `contentModerationSevereFailClosedEnabled` (content-mod S1 — gates **warning-tier** keyword BLOCK only; env `CONTENT_MODERATION_SEVERE_FAIL_CLOSED_ENABLED`, **default `true`**; OFF = emergency rollback: warning hits ALLOW + log, no `recordViolation` escalation, startup warning via `warnIfSevereFailClosedDisabled()`; **severe-tier blocking is unconditional — no flag weakens it**; read is fail-closed `getFeatureFlagSync(key, true)`; registered featureFlags.ts:194/232), `flowIntroEnabled` / `flowLifecycleEnabled` (per-flow kill gates for the two once-per-user flow-animation overlays, `FLOW_INTRO_ENABLED` / `FLOW_LIFECYCLE_ENABLED`, default `true`; see `docs/agent-context/flow-animation.md`), `gatheringRoomEnabled` (集结房间 pre-event waiting room — entry CTAs on pool-group-detail / squad-unboxing plus the room page itself; env `GATHERING_ROOM_ENABLED`, default `false`; see `docs/agent-context/gathering-room.md`), `duoRegistrationEnabled` (default `true`, kill-switch for 双人成行 duo registration). Server-side kill switches (not exposed in auth) include `paymentsEnabled`, `registrationEnabled`, and `venueAssignmentEnabled`. Auth resolution is parallel (`Promise.all`). Admin portal `/admin/feature-flags` (super_admin only) provides toggle UI with source badges, saving state, confirmation for dangerous flags, and audit metadata. Admin mutation is logged to `admin_audit_logs` with action `FEATURE_FLAG_UPDATED`; PUT validates the key against `FLAG_ENV_MAP` and value against `z.enum(["true", "false"])`.

> **Profile V1.7 rollout note (2026-07-15):** `profileRedesignEnabled` is the server-owned parent rollback switch for `pages/profile/index.tsx`. `false` renders the compact real-data fallback and skips V1.7 gamification/equipment/personal-story requests. `profilePixelAvatarEnabled`, `equipmentRewardsEnabled`, and `personalStoryEnabled` remain independent child switches. `personalityShareEnabled` still governs personality-result sharing; it does not imply a Profile-tab share action.
