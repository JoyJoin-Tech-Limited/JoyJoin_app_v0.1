# Mini-Program Landing Page — Agent Context

> Extracted from AGENTS.md §6 (2026-07-31). Load when working on `pages/index/index`, landing hero assets, or cold-start auth routing.

**Landing page cold-start behavior (2026-06-08):**
- `AutoLoginBridge` (in `app.ts`) attempts silent WeChat auto-login for returning users on every cold start. Previously orphaned — now wired into the app component tree. Retryable errors (transport, 500) reset the attempt guard so the next mount/foreground can retry.
- The landing page (`pages/index/index`) runs a **unified redirect effect** after auth resolves: authenticated users → `nextStep`; guests with an incomplete anonymous assessment session → `reLaunch` back to `/pages/onboarding/personality-test/index`. This prevents users from being stranded on the landing page after backgrounding WeChat.
- **Returning mid-onboarding users see the welcome-back screen** (`pages/onboarding/welcome-back/index`) instead of being dumped directly into their `nextStep`. The landing page checks `shouldShowWelcomeBack()`: requires `nextStep !== 'discover'`, `features.restartOnboarding === true`, `restartsRemaining > 0`, and `joyjoin_welcome_back_seen` not set. This gives users context about their progress and an explicit choice to restart (via `POST /api/auth/onboarding/restart`) or continue.
- The welcome-back `seen` flag is reset after 7 days (`app.ts` `useLaunch` TTL heuristic) because WeChat storage persists across mini-program deletion. Without this, users who delete and re-enter would never see the welcome-back screen again.
- Primary CTA navigation uses a **5s safety timeout** (`navigateWithLegalGate` in `LandingPage.tsx`) that resets the button loading state if `Taro.navigateTo` to the onboarding subpackage hangs (e.g., subpackage download stuck). The timeout is tracked in a ref and cleared on unmount to prevent leaks.
- **Swipe-back / foreground recovery (2026-06-13):** `LandingPage.tsx` resets `isPageExiting` via `useResetOnShow` when the page is re-shown (e.g., iOS swipe-back from an onboarding page). The CTA sets `isPageExiting` before calling `navigateToMiniProgramNextStep` so the button shows a loading state during navigation. If the user has an incomplete anonymous assessment session or an authenticated `nextStep`, the primary CTA label is context-aware (`进入发现页` when `nextStep='discover'`, `继续开盒` for authenticated onboarding or a guest incomplete session — labels unified 2026-07-26) and routes to the appropriate step instead of restarting from the personality test intro.
- **Authenticated discover routing (2026-06-13):** Returning authenticated users whose `nextStep === 'discover'` are treated as a valid continue state. The primary CTA label shows `进入发现页` and routes directly to Discover (`Taro.redirectTo({ url: MINI_PROGRAM_ROUTES.discover })`) instead of pushing them through onboarding again.
- **Viewport lock + reduced-motion polish (2026-06-23):** The landing page uses a locked `100dvh` shell with `disableScroll: true`; the content zone is flex-constrained with `ResponsiveSpacer` (`collapseBelow={640}`) so the fixed bottom CTA stays reachable on short phones. CTA glow and auth-hint entrance animations are suppressed under `prefers-reduced-motion: reduce`.

**Landing blind-box city redesign (2026-07-26, PM-reviewed 18/24 基本达标 same day):** The landing screen is repositioned as the "盲盒城市体验平台" poster — CSS dusk gradient (`$color-dusk-*` tokens in `_variables.scss`, cream → lavender), one Lovart master composite (glowing box + peeking Xiaoyue) + 5 floating transparent sprites (buildings/cards/map-pin/dice/glass — dinner/game + city-exploration dual coding, no NPCs) under `/assets/lovart/landing/`. Assets were generated via `docs/design/lovart-brief-landing-blind-box-city-20260726.md` and processed by `npm run build:landing-hero-assets -w mini-program` (purple −15% sat correction, WebP q80+alpha, 48² LQIP; sprite trim/fit) and gated by `npm run check:landing-hero-assets -w mini-program` (43 checks: geometry/alpha/edge-decay/float-zone/saturation/neon/weight). The hero art style was A/B-tested: **2D low-poly painterly illustration (the app's established Lovart language) won over 3D clay render**. Bundled welcome mascot is the CDN-failure fallback (sprite-level failures remove that sprite silently). Copy: headline「这座城市，为你藏了一局」(accent `$color-primary-dark` — `$color-primary` fails 4.5:1 on the mist stop), subtitle「答几道小题，悦仔替你攒一桌聊得来的人」(opacity 0.85 — 0.75 failed contrast on the deep stop); "命格" retired from landing. CTA labels: `拆开我的盲盒` (new) / `继续开盒` (continue) / `进入发现页` (discover); dimmed-but-not-dead disabled state (opacity 0.65, sheen suppressed); a faint golden sheen sweeps the primary CTA every 8s (`cta-sheen-sweep`, removed under RM/low-end) carrying the box glow to the tap. The personality-test intro carries the one-line bridge「先装盒，再拆盒：几道小题，把你的样子装进去。」(`personality-test__intro-bridge`). Entrance is a ~1.2s CSS choreography gated by `--entered` (hero-land → halo-bloom → sprite stagger → clip-path `hero-peek` → copy rise → CTA); idle = 4s breath + halo pulse + 3 particles; low-end (`--low-end`) kills infinite loops AND skips `hero-peek` (clip-path animates on the main thread in WKWebView — perf WARN-3); `--rm` forces static + 150ms fade. `hero-peek` `from` inset = 52% (measured on the shipped master: head y≈23–50%, box body dominant from y≈53%). Hero assets are preloaded during the BoxLogoEntryScreen splash window via `LANDING_HERO_PRELOADS` in `routePreloadAssets.ts` (perf WARN-1 — first request no longer waits for LandingPage mount); `check:landing-hero-assets` is wired into `build:weapp` (perf WARN-4). Time-to-CTA ≈2.65s (entry splash 1.1s + choreographed 1.06s bottom-zone delay) is a **documented trade-off** (perf WARN-2): the landing is seen almost exclusively by new users on first cold start — returning authed users redirect away — so the entrance movie is the product's first impression by design. Sprite slot geometry is fitted to the shipped master (head region x≈341–650rpx/y≈158–344rpx on stage — dice sits in front of the box's right shoulder, never over the ear). Short screens (<640px window height) get `--short` (stage scale 0.85, headline 48rpx). `PhaseIconCarousel` (+ test/SCSS) and the bundled `landing-phase-icons/` are retired (−80KB main package); nav bar is now `#FFFAF4`/black to blend with the dusk gradient. Analytics: `landing_cta_tap` / `landing_hero_asset` / `landing_dwell` via `lib/analytics/landingAnalytics.ts` → `POST /api/analytics/landing` (server whitelist in `routes/domains/analytics.ts`, route tests in `landingAnalyticsRoutes.test.ts`). All resilience behavior is preserved: auth gate, offline/timeout banners, legal shake, login row, test-login, 5s nav timeout, `useResetOnShow`. H5-render review loop: `npm run screenshot:landing` (guest-forced 401 route intercept; generator clicks legal to light the CTA). **Gotchas fixed during render review:** `BrandLogo` inline-rpx sizing balloons in H5 — landing now sizes it via `.landing-page__brand-logo` SCSS `!important` class; `.landing-page__legal-checkbox` needs `position: relative` or the ✓ mark escapes to the row.

**Landing mechanism-first choreography — E3 盒子吐卡 (2026-07-31, shipped 2026-07-31):** The landing hero gained a hero-performed "盒子吐卡" mechanism explaining the product in one shot (dice + 6 archetype cards converge into the blind box → box shakes → 6 archetype heads burst out into a 120rpx mechanism-strip between hero-zone and hero-text → strip connector grows to a map-pin → tap the hero replays the whole mechanism). Implementation lives in `LandingPage.tsx` + `pages/index/index.scss` (`mechanism-strip`, `cards-converge`/`dice-converge`/`box-shake`/`burst` keyframes) with `mechanismBurst.ts` (`computeBurstOffsets`, tested) computing seat offsets via `createSelectorQuery` bounding rects (box-mouth anchor + 6 seats; boundingClientRect returns post-transform coords so the `--short` 0.85 stage scale is handled). Timings: cards converge 420+260ms, dice 500+260ms, box shake 780ms, burst settle 520+80+460+stagger. Tiers: `--rm` (static, fade only), `--low-end` (seat-fade stagger instead of full burst, infinite loops killed), `--short` (stage scale 0.85). A `?freeze=burst` capture param holds the mechanism at the settled state (frozen seats keep opacity 1) for screenshot reviews; the hero tap replays the mechanism (animation keys re-triggered) and tracks `landing_mechanism_replay` (added to the server analytics allow-list in `routes/domains/analytics.ts`). Landing caption deduped to 「① 答小题 · ② 攒一桌 · ③ 见真人」 (2026-08-01). Known teleport bug fixed 2026-08-01: the seat fly-back transition must live on the BASE `.mechanism-strip__seat` rule (not the `--settled` variant class) because transitions are governed by the after-change computed style — class removal previously teleported seats back to the box instantly.
