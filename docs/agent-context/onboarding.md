# Mini-Program Onboarding Patterns — Agent Context

> Extracted from AGENTS.md §6 (2026-07-31). Load when working on onboarding pages, personality test, essential-data, or edit-profile. Skill: `onboarding-state-architecture`.

**Flow 1 intro overlay (2026-07-31, revamp 2026-08-03):** `pages/onboarding/profile-review/index.tsx` plays the once-per-user `joyjoin-intro` flow overlay after the UnboxingCeremony completes (the packed-box entrance beat is skipped via `FlowShell entranceMode='cut'` because the ceremony already delivered the box metaphor). The revamp wires the archetype-personalized subline, adds a detail-page forward CTA, refreshes copy in `packages/shared/src/copy/flowAnimationCopy.ts`, and runs the full Rendered-Truth Visual Gate clean. Gated by `features.flowIntroEnabled`; seen-state key `joyjoin_flow_seen:v2:joyjoin-intro:<userId>`. Details: `docs/agent-context/flow-animation.md`.

**Onboarding restart v0.1 (2026-05-23):** Returning users with partial onboarding may see a welcome-back screen (`/pages/onboarding/welcome-back`) offering to continue or restart. Restart clears all onboarding-derived data including V4 assessment sessions/answers (preserves WeChat identity + phone), resets to `personality-test`, and burns one of 5 lifetime restarts. Gated by `features.restartOnboarding` (DB-backed feature flag) in auth response. Endpoint: `POST /api/auth/onboarding/restart`. Idempotent — double-tap does not consume quota.

**Accessibility patterns in personality-test results (2026-06-05 / updated 2026-06-22):**
- **Reduced motion:** CSS `@media (prefers-reduced-motion: reduce)` plus a JS-driven `.personality-results--reduce-motion` container class suppresses stagger entrances, holographic shimmer, and card tilt. The slot-machine animation itself is intentionally mandatory and is not gated by `reduceMotion`; the only bypass is the server-driven `personalitySlotAnimationEnabled` feature flag.
- **Sprite animator crossfade gating:** `XiaoyueSpriteAnimator` crossfade between expression sprites is suppressed when `reduceMotion` is active or when `useDeviceTier()` reports a degradation-tier device. Both the entering and exiting sprites share the same `motionEnabled` gate so a low-end or reduced-motion device never plays one side of the crossfade while the other is frozen.
- **Sprite animator animation engine (2026-06-22 / revised 2026-06-22):** `XiaoyueSpriteAnimator` uses JS-driven `setInterval` with `useState` frame index and `useEffect` cleanup — CSS `steps()` was attempted but reverted because CSS custom properties inside `@keyframes` are unreliable in WeChat runtime (causes horizontal sliding instead of stepping). A `pendingExitCleanupRef` pattern defers crossfade cleanup when the app is hidden (handled via `handleAppHide`/`handleAppShow`), preventing stale DOM state on return. The `animationGen` key on `SpriteFrame` forces remount on state changes (replaces the removed `playKey` pattern). Background tint `rgba($color-primary, 0.04)` on `&__frame` masks the loading gap before the sprite sheet decodes.
- **Slider accessibility:** The `slider` question's live value badge uses `aria-live="polite"` so screen readers announce value changes without overwhelming the user. The badge transform is gated by JS `reduceMotion` and returned as inline `transform` (e.g. `translateX(...) scale(...)`); CSS custom properties for per-frame updates were removed because they are unreliable in the WeChat runtime. `will-change: transform` is applied for GPU-composited drag. A first-time hint `"拖动滑块，选择最符合你的程度"` dismisses on first interaction.
- **Touch-tilt rAF throttling:** The collectible card's touch-drag tilt uses `rafPendingRef` + `pendingTiltRef` to batch `setTouchTilt` calls to a single `requestAnimationFrame`, preventing React state flood during fast swipes.
- **Offline resilience:** `Taro.getNetworkType()` detects offline state on fetch failure. `ErrorStage` shows offline-aware copy (`'网络好像断开了'` vs `'揭晓过程被打断了'`). Retry uses exponential backoff capped at 4s (`Math.min(4000, 1000 * 2^(retryCount-1))`) with `retryTimerRef` cleanup on unmount.
- **Predictive prefetch:** Primary archetype image is preloaded via `preloadImagesWithDiagnostics` on test completion (from the final answer response) and again on results page mount, reducing perceived load time.
- **Timer cleanup:** `FinalStage` tracks its detail-sheet close-animation timeout in `detailCloseTimerRef` and clears it on unmount to prevent setState-after-unmount.
- **Error-state screen-reader support:** `ErrorStage` uses `role="alert"` and `aria-live="polite"` so assistive tech announces sync failures.
- **Split-brain hardening:** Server `validateFinalResult()` validates `primaryArchetype` before persistence; client `isValidFinalResult()` blocks transition if invalid. Unified fallback chain ensures slot target and display archetype resolve identically.
- **Accent colour rule:** Whenever an archetype name appears inline with plain text, render it in the archetype's branded `accentText` colour (computed via `getContrastSafeArchetypeColor()`) with zero separator space (e.g. `典型<span style="color:accentText">柯基</span>`). Applied to hero badge, blend line, bridge card, skill chip, and poster input.

**Essential data onboarding patterns (2026-06-05):**
- **Picker `aria-label` pattern:** All picker trigger buttons have dynamic `aria-label` that reflects current selection state (e.g., `出生年份：1990 年` vs `请选择出生年份`). Copy this pattern for all future picker implementations.
- **Intent grid memoization:** The 6-option intent grid is wrapped in `useMemo` with stable dependencies (`intentOptions`, `intent`, `toggleIntent`) to prevent re-render on unrelated state changes (mascot reactions, step transitions).
- **Picker center alignment:** Picker trigger Views use `justify-content: center` with absolutely-positioned checkmark (`position: absolute; right: 24rpx; top: 50%; transform: translateY(-50%)`) to maintain visual centering regardless of checkmark presence.
- **Schema resilience:** `updateFullProfileSchema` in `packages/shared/src/schema/_definitions.ts` accepts `industryConfidence` as `z.union([z.string(), z.number()]).optional()` to handle both string and numeric confidence values from the profession classification API.

**Personality test re-entry safety (2026-06-04):**
- **Archetype hard guard:** `pages/onboarding/personality-test/index.tsx` checks `auth.user?.primaryArchetype` on mount. If the user already has an archetype, it immediately redirects to `/pages/discover/index` via `Taro.redirectTo`. This prevents users with completed tests from accidentally re-entering the assessment.
- **Stale-session cleanup (server):** `POST /api/assessment/v4/start` detects incomplete sessions where `selectNextQuestion` returns `null` (all questions answered but `completedAt IS NULL`). It marks the stale session `completed`, calls `markPersonalityTestComplete(userId)`, then starts a fresh session. Wrapped in defensive try-catch to prevent startup 500s.
- **Client `completing` phase error UI:** If the final submission API fails while in `completing` phase, the page renders a Xiaoyue `actionFailure` visual with warm copy ("同步遇到小状况 / 悦仔马上帮你重试~"), `role="alert"` + `aria-live="polite"`, and a retry CTA with `haptics('light')`. This replaces the previous silent loading-shell hang.
- **Completing-phase error recovery (2026-06-13):** On final submission failure the page returns the user to the testing phase (with their last answer pre-filled) instead of dumping them back to the intro, so retrying resumes where the failure occurred. Raw API errors are mapped through `getErrorMessage` baselines before display.
- **Test-page architecture + zero-scroll lock (2026-06-23):** The main orchestrator (`pages/onboarding/personality-test/index.tsx`) stays below the 1200-line frontend threshold by delegating intro, question, preload, and completing-error rendering to focused sub-components (`PersonalityTestIntro`, `PersonalityTestQuestion`, `PersonalityTestPreloadLayer`, `PersonalityTestCompletingError`) with shared types in `types.ts`. The intro and testing phases use locked `100dvh` shells with explicit inner `ScrollView` ports; the answer zone is a Taro `ScrollView` (not CSS `overflow-y`).

**Edit-profile design patterns (2026-05-24):**
- **2-step grouping:** Step 1 "基础档案" = static facts (name, gender, birth, city, hometown region/city via `hometownRegionCity`, education). Step 2 "社交画像" = expressive choices (profession via AI overlay, intent grid, interests). Maintains cognitive continuity with onboarding.
- **Validation + scroll-to-error:** Inline field validation with `fieldErrors` state. First error field id passed to `ScrollView scrollIntoView`.
- **Unsaved changes guard:** `Taro.enableAlertBeforeUnload({ message: '…' })` when `hasChanges` is true; `Taro.disableAlertBeforeUnload()` on cleanup.
- **Skeleton loading:** `isInitializing` gates a skeleton block with shimmer animation; disabled under `prefers-reduced-motion`.
- **Floating CTA bar:** Fixed bottom white bar with subtle top shadow (`rgba($color-text-primary, 0.04)`).

**Blind-box onboarding continuity — steps 6-8 (2026-07-31 → 2026-08-01):** Onboarding now runs a continuous blind-box metaphor from landing → completion.
- **BoxJourneySpine** (`components/onboarding/BoxJourneySpine.tsx+.scss`): 64rpx macro-journey spine — box glyph fills `scaleY(step/3)` via inline style + `from`-only keyframes (one animation serves all fill levels), lid closes at step 3 (sealed) with landed-gold glow dot; 0.75 opacity so it never reads as a second progress bar; short screens (<640px) drop the minute hint; RM tier kills the fill animation. Placement: essential-data (row above `FormStepper`), extended-data (replaces the eyebrow pill), profile-review (replaces eyebrow, centered via page override). Subpackage trap: all 3 page SCSS must `@use` the component SCSS (verified in compiled dist WXSS).
- **Voice matrix** (`packages/shared/src/copy/onboardingVoice.ts`, exported from copy barrel): `getOnboardingVoiceLine(stepId, archetype)` → Tier A 12-archetype × 8-step matrix (96 lines) with Tier B step defaults as graceful fallback; keyed override map so missing keys degrade silently. Completeness test in `copy/__tests__/onboardingVoice.test.ts` (12×8 coverage, Tier B fallback, zero-emoji rule). Wired: essential-data coach bubble per-step (`essential-${stepConfig.id}`), extended-data opener, profile-review coach line. `STEP_CONFIG.mascotMessage` field is **deleted** — voice matrix is the single copy source. `stepIds.ts` (`ESSENTIAL_DATA_STEP_IDS`) is the canonical step-id source of truth; `essentialDataVoiceMap.test.ts` locks STEP_CONFIG↔voice-key parity.
- **Tinted chrome:** `FormStepper` gained optional `accentColor` prop (inline background on filled segments); spine tinted via `getContrastSafeArchetypeColor` / `archetypeVisual.accent`. Gotcha: `getArchetypeVisual().accent` returns `rgba(r,g,b,1)` — the `${accentColor}20` string-concat alpha pattern produces INVALID CSS (silently dropped); use `accentText` for text-on-light instead.
- **UnboxingCeremony** (`components/onboarding/UnboxingCeremony.tsx+.scss`, phase 3, 2026-08-01): completion overlay at profile-review — fixed inset-0 dusk-gradient overlay reusing squad-unboxing layered Lovart box art (`BLIND_BOX_BODY/LID/INTERIOR` via `lib/mascot/blindBoxAssets`, CDN, zero new assets). Choreography: halo bloom → lid lift (520ms transform) → interior glow → compact 入场卡 rises (archetype headlines in display font + `accentText` tint, sub-line「{name} · 入场卡已生效」, hint「轻触继续，去看为你准备的局」— factual, no fabricated 攒局 promise) → tap or 2.4s auto-advance fires `onComplete`. RM tier = static card + 150ms fade; low-end drops the interior glow layer. `_reveal-motion.scss` holds the shared reveal language (`$ease-reveal-rise`, `$ease-reveal-pop`, `reveal-rise-in`/`halo-bloom`/`pop-settle` keyframes) so new payoff moments mirror the reveal instead of duplicating it.
- **Completion flow (profile-review):** API success → `isCelebrating` + haptics + 500ms → `setShowCeremony(true)`; `handleCeremonyComplete` (tap/auto) → `invalidateAuth` → `getUserState` → intro-flow check or navigate. **Race fix (2026-08-01):** `handleCeremonyComplete` sets `isPageExiting(true)` synchronously before `invalidateAuth` so `useAuthGuard` can't fire a competing `switchTab` that kills the first-run intro flow; post-ceremony calls wrapped in `withTimeout(QUERY_TIMEOUT_MS=12s)`. `setShowCeremony` is wired into `useResetOnShow` (swipe-back safety) AND `useAuthGuard` suspend (ceremony is a mid-navigation state).
- **Web-runtime gotcha (2026-08-01, fixed in `_mixins.scss`):** `onboarding-page-enter/exit-transition` mixins left non-none transforms on page roots (static translate3d + forwards-fill). In browsers this makes the page root a containing block for `position: fixed` descendants, mis-centering full-screen overlays on full document height instead of viewport (H5-only; WeChat is always viewport-relative). Fix: end-state statics (`opacity:1; transform:none`) + backwards fill on enter; static `transform:none` on exit. Chrome interpolates keyframe `transform:none` as identity under forwards fill, so the fill-mode change was the actual fix.
- **Gender chip (2026-08-01):** `lib/utils/genderLabel.ts` (`getGenderLabel`) maps `male`/`female`/`男性`/`女性` → `男生`/`女生`, `null` for undisclosed. Note: `pages/profile/index.tsx` has a private duplicate of this helper — not yet refactored.
- **Copy fixes (2026-08-01):** profile-review label「来这里想…」→「来这里想要什么」; landing caption deduped (see landing-page.md).
- **Dev tooling:** `mock-h5-server.mjs` gained `POST /api/profile-review/complete` (real path; legacy `/api/onboarding/profile-review/complete` route is now unused); `screenshot-server.mjs` gained `profile-review-ceremony` capture (taps `.profile-review__submit`, captures settled overlay before 2.4s auto-advance; use `?motion=reduce` for page reveal — ceremony reads OS-level reduced-motion only). `build:weapp` wipes the H5 dist — rebuild H5 after any weapp build before screenshots.

**Personality results — Xiaoyue AI analysis reliability + prefetch (2026-08-01):** The「悦仔的解读」hero bubble (`results/FinalStage.tsx`) is LLM-generated via `POST /api/xiaoyue/analysis` (`apps/server/src/xiaoyueAnalysisService.ts`).
- **Root cause of「悦仔的解读暂时不可用」(2026-07-31):** `deepseek-v4-flash` is a reasoning model — with `max_tokens: 500` the whole budget went to reasoning tokens (`reasoning_tokens=500`, empty `content`, `finish_reason=length`), the craft gate then retried twice (~21s total) and blew past the client's 15s `REQUEST_TIMEOUT_MS` in `lib/api/api.ts`, surfacing as the fallback bubble. Server caches results 1h, so only first visits hit it.
- **Generation config (2026-08-01):** `max_tokens: 2500` + `reasoning_effort: 'low'` (measured: reasoning 28 tokens, ~2.4s) + `maxRetries: 1`. Typical generation 3-7s, worst case ~13s. In-flight dedup (`inFlightAnalysis` map + `runAnalysisGeneration()` split) prevents duplicate LLM calls when prefetch and the direct result-page call overlap.
- **Cache key:** trait scores are bucketed to the nearest 10 so a mid-test snapshot and the final profile land in the same 1h cache entry (key also carries archetype, confidence band, top-2 archetypes, and the `XIAOYUE_*_PROMPT_VERSION` suffixes).
- **Prefetch (two stages):** (1) completion-time — `triggerXiaoyueAnalysisPrefetch.ts` fires with the final result, hidden behind the slot animation window; (2) mid-test (2026-08-01) — `personality-test/index.tsx` fires during answering when `progress.answered >= 8` and `currentMatches[0].confidence >= 0.7`, throttled to 30s or a top-1-archetype change. Both hit `POST /api/xiaoyue/prefetch`; the mid-test call sends only `{ sessionId }` and the server re-derives the authoritative profile by replaying stored answers (`lib/assessmentEngineState.ts` — the same shared engine the assessment routes use), so authenticated and anonymous flows behave identically.
- **Result-page progressive render (2026-08-01):** the skeleton is gone — while the AI copy loads, the hero card shows the deterministic template bubble (「这个命格为什么像你」+ summary + hiddenStrength) with a subtle「悦仔正在补充解读」pill (`personality-results__xiaoyue-pending-indicator`, primary tint); the AI bubble swaps in on arrival. The amber「暂时不可用」pill now renders only after a real failure (`isLoadingAnalysis` settled without data).
- **Transport-level fix (2026-08-11):** `getDeepseekClient()` now injects top-level `thinking: { type: 'disabled' }` into DeepSeek requests that carry no thinking control, and the social router's `buildThinkingExtraBody('flash')` sends the same. The 2026-08-01 workaround here (`reasoning_effort: 'low'`) stays valid — `thinking: disabled` + `reasoning_effort: 'low'` is accepted (0 reasoning tokens, content direct). Canonical details: `docs/ai/AI_MODEL_ROUTING_STRATEGY.md`.
