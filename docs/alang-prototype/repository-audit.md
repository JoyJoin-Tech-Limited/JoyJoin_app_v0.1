# 闪现 NPC｜阿浪 V1.7 — 当前实现差异审计

> 审计日期：2026-07-14
>
> 产品基准：`JoyJoin_Master_PRD_V1.7_Codex执行版_强调Mockup未完全落地.docx`
>
> Scope ID：`ALANG-V17-VISUAL-ALIGNMENT`
>
> 代码范围：Mini Program、Server、Shared；不改盲盒、匹配、支付、现有 Tab 架构

## 结论

阿浪已具备服务端剧情状态机、5 米稳定到达、故事归档、断点恢复和受控复测能力，但“路由存在/功能可用”不等于视觉验收完成。按 V1.7 Word、当前 H5 验收截图与代码逐项比对，Profile、Discover 闪现卡、阿浪搜索页、我的故事页目前均为 **F3（视觉基本一致）**，没有一页达到 F4。

未达到 F4 的共同原因是：阿浪正式人物/场景图仍为 `awaiting-approved-art`；H5 截图不能替代微信原生 Map、TabBar、安全区与真机定位验收；部分 Word Mockup 的角色、城市背景和独立故事插画是设计目标示意，不是已批准可直接入库的资产。所有 fallback 必须继续明确标注“场景示意”，不得对外宣称正式美术已落地。

### Fidelity 判定口径

| 等级 | 判定 |
| --- | --- |
| F0 | 页面或入口未实现 |
| F1 | 功能已接入，但结构与目标差距明显 |
| F2 | 主要结构接近，视觉层级、资产或关键状态仍不完整 |
| F3 | 结构、数据与主要视觉层级基本一致；仍有正式资产或真机验收差异 |
| F4 | 正式资产、全部状态、同尺寸截图与微信真机均通过验收 |

> 本文不把 F1/F2 写成“已完成”。四页的 F3 只表示本轮可验证范围已达到“视觉基本一致”，不是最终设计验收。

## 阶段 A｜编码前差异矩阵

### 1. Profile / “我的”页（APPROVED TARGET 06）

| 审计项 | 编码前结论 |
| --- | --- |
| Current State | 旧版以浅色资料卡和功能列表为主；人格形象、成长数据、故事入口、徽章与服务入口没有形成一个与 V1.7 相同的身份舞台。`profileRedesignEnabled` 已存在，但视觉回滚与 V1.7 数据请求的边界需要收紧。 |
| Target Mockup | V4 人格形象主视觉；真实潮流值、活动数、连接数、资料完成度；“我的故事”、伙伴/装备同级入口、徽章区、服务入口、设置入口和既有底部 TabBar。开关为 `true` 时显示 V4，`false` 时显示真实数据简洁页。 |
| Structural Gap | 缺少“身份舞台 → 三项统计 → 成长档案/我的故事 → 徽章 → 更多服务”的主次结构；伙伴/装备没有目标层级入口；开关关闭时仍可能保留与 V1.7 视觉有关的后台请求。 |
| Visual Gap | 缺少目标稿深色高对比人格 Hero、潮流值进度、独立统计卡、故事横幅和徽章/服务分区；旧页面的信息密度与视觉重心偏散。 |
| Asset Gap | V4 十二人格角色图已有批准资产；Word 中的蜘蛛角色、霓虹城市、完整装备套装是示意或其他人格示例，不能直接当作当前用户资产。阿浪故事横幅正式场景图未获批。 |
| Data Gap | 需要接真实 XP/等级、活动、连接、资料完成度、故事档案与徽章数据；不能照抄 Mockup 的 `680`、`12` 等样例。开关关闭时必须停止潮流值/成长与阿浪档案请求。 |

### 2. Discover 闪现卡（ACTIVE 03）

| 审计项 | 编码前结论 |
| --- | --- |
| Current State | Discover 已有阿浪入口和真实任务状态，但旧卡片主要是一条隐私说明，比例、标题/副文案/CTA 层级及继续状态表达与 ACTIVE 03 不足以视为视觉落地。 |
| Target Mockup | 只校正现有 Discover 中的紧凑单 NPC 闪现卡：明确“闪现 Beta”、阿浪故事标题、副文案、三个说明 chip 和一个主 CTA；与正式活动卡区分，不让地图、距离或时间抢主视觉，不重做整页 Discover。 |
| Structural Gap | 缺少三段短说明、进行中优先文案与统一单 CTA；卡片内容层级需要压缩到一个可扫描入口，而不是任务详情或地图预览。 |
| Visual Gap | 卡片图文比例、留白、主标题权重和 CTA 面积与目标有差距；旧隐私条过长，不能承担 Target 中的轻量状态标签作用。 |
| Asset Gap | 正式阿浪人物/场景横图未获批。Word 参考中若出现多 NPC 城市地图，只能作为整体气氛参考；它与 FUTURE 04/REMOVED 09 冲突，不能复刻为功能或地图资产。 |
| Data Gap | 标题、进度和 CTA 必须来自真实 mission/myProgress；未开始与继续中需要分支，不能写死“继续”或伪造附近角色数量。 |

### 3. 阿浪搜索页（ACTIVE 05）

| 审计项 | 编码前结论 |
| --- | --- |
| Current State | 目标坐标已保密，原生 Map 只显示用户，距离与定位状态已有真实数据；但地图和功能状态仍偏工具化，区域横幅、距离主视觉、雷达层级和“找到后”说明没有完整对齐 Mockup。 |
| Target Mockup | “区域提示 → 静态雷达/真实距离与信号 → 找到后会发生什么 → 辅助地图”的顺序。距离数字为主视觉；腾讯地图只辅助确认用户所在位置；不显示精确目标 Marker、路线或搜索目标坐标。 |
| Structural Gap | 缺少首屏区域提示卡、静态非地理雷达、找到后行动说明及可控的辅助地图入口；异常/授权恢复需要留在主流程附近。 |
| Visual Gap | 真实距离和信号强度不够突出；地图容易抢占首屏；定位精度、靠近提示、CTA 与 5 米到达规则的层级需要重新组织。 |
| Asset Gap | 正式区域横幅与“找到后”阿浪场景图未获批；Word 中阿浪角色与南头古城插画不能直接从 Mockup 导出使用。 |
| Data Gap | 距离、精度、定位状态和服务端 stage 必须是真实值；搜索阶段响应不得包含 target/routeDestination。Mockup 的 `84m/300m` 仅为示例，不能成为写死数据。 |

### 4. 我的故事页（ACTIVE 07）

| 审计项 | 编码前结论 |
| --- | --- |
| Current State | 已有 archive 列表与详情跳转，但缺少真实汇总、筛选、时间线和“继续中的故事”层级；列表更接近任务记录，未充分表达用户经历路径。 |
| Target Mockup | 真实档案统计、全部/继续中筛选、时间线、故事状态、地点与完成时间；已收录故事和进行中任务分开呈现，形成“仍可继续”的故事感。 |
| Structural Gap | 缺少顶部统计、双 Tab、继续中的 mission 数据源、时间线节点及针对空/错/加载状态的内联恢复。 |
| Visual Gap | 原列表缺少时间轴和统一的标题/地点/时间/情绪标签层级；顶部没有故事总览视觉，难以形成个人档案感。 |
| Asset Gap | 正式故事总览与单故事场景图未获批；V1.7 明确不新增“每条故事独立缩略图”字段，因此不能用重复插入或伪造图片 URL 模拟 Mockup。 |
| Data Gap | 需要从真实 archives 计算收藏数/地点数，从 missions 读取进行中数量、stage 和进度；不能照抄 Mockup 的 `12/3/680` 或伪造多条故事。 |

## 阶段 B｜实现后验收矩阵

| 页面 | 已一致项 | 剩余差异 | Placeholder / 场景示意 | 技术限制 | Fidelity |
| --- | --- | --- | --- | --- | --- |
| Profile / “我的” | 已形成身份舞台、V4 动态人格、真实潮流值/等级、活动数、连接数、资料完成度、故事入口、伙伴/装备同级入口、徽章区、服务与设置入口；保留既有 TabBar。`profileRedesignEnabled=true` 显示 V4；`false` 显示真实数据简洁页，并停止 V1.7 gamification/archive 请求。 | Word 中的霓虹城市背景和完整四件装备陈列未复刻；完整装备体系属于 FUTURE 08。不同人格会使用真实用户对应角色，不会固定成 Mockup 的蜘蛛。 | Profile 人格角色使用已批准 V4 资产；“我的故事”横幅仍使用明确标注“场景示意”的 fallback。 | H5 截图不能验证微信自定义 TabBar、状态栏/安全区和真实长列表滚动；真实数据会与 Mockup 样例值不同。 | **F3** |
| Discover 闪现卡 | 已落地紧凑单 NPC 卡、`闪现 Beta`、故事标题/副文案、三个说明 chip、未开始/继续中 CTA；与正式活动卡区分，卡内没有距离、时间、地图或目标坐标。H5 全页上下文同时验证了卡片位于“探索体验”标题后、正式活动卡前。 | 尚无获批阿浪人物/场景横图；Word 全屏参考中的多 NPC 地图构图没有复刻，因为属于明确排除范围。 | 当前使用通用礼盒角色场景，并显示“活动场景示意”。 | 已补齐 750×1624 全视口证据；H5 仍不能验证微信原生 TabBar，且 Word 内嵌参考图本身不是统一设备尺寸。 | **F3** |
| 阿浪搜索页 | 已按“区域提示 → 雷达/距离 → 找到后说明 → 辅助地图”排序；距离数字为主视觉，展示真实定位信号/精度；Map 只显示用户本人，搜索响应不含目标坐标，服务端 stage 仍纠正陈旧 URL。 | 正式南头古城/阿浪角色插画未获批；与 Mockup 的角色造型和场景质感仍有可见差异。 | 区域横幅和找到后插画分别标注“区域场景示意”“找到后场景示意”。 | H5 只验证折叠地图入口，不能渲染和验收微信原生 Map、定位授权、前后台 GPS、5 米稳定到达；这些必须真机验证。 | **F3** |
| 我的故事 | 已使用真实 archive/mission 数据显示收藏数、继续中数量、地点数；完成“全部故事/继续中的故事”筛选、时间线、地点/时间/情绪标签、真实进度和加载/错误/空状态恢复；不把 mission 写成流水账。 | 缺少获批的故事总览与每段故事场景图；少量真实档案时页面会自然留白，不用伪造记录填满 Mockup。 | 总览与卡片复用受控 fallback，并分别标注“故事总览场景示意”“故事场景示意”。 | 按范围不新增独立缩略图字段，因此多个档案暂时共享 fallback；H5 样例数据不能替代真实 API、长列表与微信滚动验收。 | **F3** |

### F4 阻断项

1. 设计团队提供并批准阿浪事件横图、搜索区域图、找到后图、结果/故事图，并更新 manifest 状态；同时提供 Profile Hero 背景，或签字接受当前深紫渐变方案。
2. 在微信开发者工具及至少一台 iOS、一台 Android 上验证 TabBar、安全区、字体、长列表、定位授权、原生 Map、弱网和前后台恢复。
3. 正式数据联调后复核长标题、零档案、多档案、进行中和错误恢复状态。

> 已完成的截图基线：四页统一使用 375×812 CSS viewport、2× device scale，产出 750×1624 PNG；Discover 为包含上方页面层级和下方正式活动卡的全视口，不再是卡片裁切。Word 中四张参考图的原始像素比例不同，因此 F4 仍以设计指定设备与微信真机验收为准，不能靠拉伸参考图伪造“同尺寸”。

## 其他功能、安全与复用状态

| 范围 | 当前状态 |
| --- | --- |
| 服务端状态权威 | `GET /api/alang/missions/:slug` 的 `myProgress`/archive 是阶段权威；搜索、对话、陪伴、结果均会从陈旧 URL 纠正到服务端阶段。 |
| 结果与归档 | `stage=result` 先展示结果，不自动归档；用户主动收录后生成 archive，刷新仍从服务端恢复。 |
| 同账号复测 | `POST /api/alang/debug/missions/:slug/reset` 在事务内只删除当前 acting user、指定 mission 的 progress 及其对应测试 archive；幂等返回删除数量，不能通过 body 传入其他 userId。 |
| Debug 权限 | Reset 在非 single-test、production 或 `APP_MODE` 缺失时返回 **403**；其他内部 Debug 路由继续按安全策略返回 **404**。服务端同时验证 `alangEnabled` 与 single-test 条件，不能只靠前端隐藏。 |
| 地图复用 | 继续使用 Taro/WeChat 原生 `<Map>` 和现有 `/api/geo`；腾讯 WebService 继续复用 `TENCENT_MAP_KEY`，没有新增 SDK、provider、Key 或第二套坐标转换。 |
| 坐标与到达 | 公共 DTO 使用 GCJ-02 `latitude/longitude`；旧 `{lat,lng}` 只在读取边界归一化。JoyJoin 服务端固定 5 米连续稳定判定仍是到达真值；腾讯路线只负责辅助距离/ETA。 |
| 搜索保密 | 搜索阶段不向客户端发送目标、剧情 GPS trigger 或陪伴终点，也不渲染目标 Marker/circle/polyline。`routeDestination` 只从 companion 阶段开始披露。 |

## 正式素材与场景示意清单

| 使用位置 | 当前资产状态 | 升级条件 |
| --- | --- | --- |
| Profile V4 人格 Hero | 已批准 V4 十二人格角色图；按真实用户人格选择 | 无需替换成 Word 中固定蜘蛛角色 |
| Profile “我的故事”横幅 | `awaiting-approved-art`，标注“场景示意” | 获批故事横幅 WebP + manifest 更新 |
| Discover 闪现卡 | `awaiting-approved-art`，标注“活动场景示意” | 获批阿浪事件横图 WebP |
| 搜索区域横幅 | `awaiting-approved-art`，标注“区域场景示意” | 获批区域图 WebP |
| 找到后说明卡 | `awaiting-approved-art`，标注“找到后场景示意” | 获批阿浪状态图 WebP |
| 我的故事总览/卡片 | `awaiting-approved-art`，标注“故事总览场景示意”或“故事场景示意” | 获批共用故事图；本轮不增加独立缩略图字段 |

## 明确不在 V1.7 本轮执行范围

- **FUTURE 04**：多 NPC 地图/附近角色地图。
- **FUTURE 08**：完整伙伴、装备陈列与装备管理页；Profile 只保留同级入口和“装备筹备中”。
- **REMOVED 09**：探索地图；不得恢复路由、导航、组件或引用 Mockup 地图来变相实现。
- 精确搜索路线、目标 Marker、多 NPC 探索、第二套地图 client/Key/坐标转换。
- 多故事独立缩略图字段、重复 archive 伪装复测、测试历史列表与批量重置。

## 仓库级已知门禁

- 微信编译已完成 868 个模块；P0 复测代码合入后的 Alang 子包实测为 **158.7KiB**，低于 1.8MB 门禁。
- 当前最终干净提交的实际主包 raw 文件树为 **3.268MiB**；现有检查器漏算部分根文件后选取的 raw 为 **3.215MiB**，本地 Deflate 估算为 **1.928MiB**，尚未获得微信官方上传预检结论。正式状态仍为 BLOCK；最大来源是约 1.46MB 的共享 assets。该问题早于本轮阿浪视觉改动，需要按主包审计单独治理，不能通过删除 Alang 子包规避。
- `check-package-size.mjs` 已按每个子包分别比较上限，不再把所有子包的合计体积误报为某一个子包超限。

## 完成度全流程审计

前置结果：`ui-layout-audit` 以四张 750×1624 H5 截图人工逐项核对为 **56/68**；`frontend-design-audit` 五维合计 **16/20**，且启发式扫描对 Profile、Discover 阿浪卡、搜索页、故事页均为 0 error / 0 warning。正式美术缺失与微信真机状态不能被自动扫描抵消。

| # | 维度 | 分数 | 证据 / 缺口 |
| ---: | --- | ---: | --- |
| 1 | Functional completeness | 4/4 | 真实主流程、断点恢复、错误恢复、复测防重与服务端权限均有专项测试 |
| 2 | State completeness | 3/4 | 加载/错误/空/继续/完成已实现；未取得所有状态的微信真机截图 |
| 3 | Copy completeness | 4/4 | 定位、保密、确认、失败恢复与场景示意文案明确，不暴露开发字段 |
| 4 | Interaction completeness | 3/4 | CTA、loading、防重复、触觉与 reduced-motion 已接；真机返回手势/原生 Map 尚待验收 |
| 5 | Delight completeness | 3/4 | Profile 身份舞台和故事时间线有情绪层级；正式阿浪人物/场景图缺失 |
| 6 | Flow completeness | 4/4 | Discover → 配置 → 搜索 → 对话 → 陪伴 → 结果 → 收录 → 我的故事闭环，复测后重新配置 |
| 7 | Accessibility completeness | 3/4 | 88rpx 触点、ARIA、reduced-motion、安全区已覆盖；大字体和多机型仍属外部验证 |
| 8 | Taro discipline | 4/4 | Taro 原生 Map/定位、现有子包和 GCJ-02 路径；无第二套地图 SDK/Key/转换 |
| 9 | Visual finish | 3/4 | 来自 56/68 布局审计；四页统一截图、层级已对齐，正式场景图阻断 F4 |
| 10 | Brand soul | 3/4 | V4 人格、品牌字体/色彩/语气一致；通用礼盒 fallback 仍弱于批准目标美术 |
| 11 | Operational completeness | 4/4 | `profileRedesignEnabled` 回滚、Alang/single-test 双门禁、reset 审计日志与最小删除范围完整 |
| **总分** |  | **38/44** | **坚稳（29–38）** |

### ROI 排序缺口

| 优先级 | 缺口 | 影响 | 工作量 | 象限 | 处理建议 |
| ---: | --- | ---: | ---: | --- | --- |
| 1 | 微信原生 Map、TabBar、安全区、授权/弱网和前后台恢复的 iOS + Android 真机矩阵 | 5 | 3 | Do first | 进入 F4 前执行；不以 H5 代替 |
| 2 | 阿浪事件横图、区域图、找到后图、结果/故事图仍未获批 | 5 | 4 | Schedule | 等设计审批后替换 manifest；不得自行生成正式素材 |
| 3 | 主包测量链和 2MB 余量 | 5 | 5 | Schedule / 独立项目 | 按 `docs/package-size/main-package-audit.md` 分批治理，不混入本轮业务行为 |
| 4 | 长标题、零档案、多档案、继续中与错误态的正式 API 真数据联调 | 4 | 3 | Do first | 上线前用 staging 数据复核 |
| 5 | 每故事独立缩略图 | 2 | 5 | Skip | 明确不在本轮范围，只保留后续 schema 建议 |

```text
           Impact ↑
           ┌─────────────────────┐
  Do first │ 真机矩阵 / 真数据态 │ 正式美术 / 主包治理  Schedule
           ├─────────────────────┤
Low-hanging│ —                   │ 独立缩略图字段       Skip
           └─────────────────────┘  Effort →
```

完成度结论：**本轮 F3 范围可提交；F4 仍不可宣称完成。**

## 性能审计

| 维度 | 分数 | 证据 |
| --- | ---: | --- |
| 流畅度 | 7/10 | 仅固定 4 格信号条使用 `.map()`；无大列表、Canvas 或非合成动画告警，动画均有 reduced-motion；无真机帧轨迹故不高估 |
| 速度 | 7/10 | Alang 保持子包、Profile 关闭新版时停止 XP/archive 请求；H5 与 WeChat 均完成真实生产编译；无真机 TTI 轨迹 |
| 设备适配 | 7/10 | rpx、ScrollView、安全区、原生定位/Map 和 reduced-motion 路径齐全；仍待多机型验证 |
| 内存安全 | 7/10 | 本轮无 Canvas、长驻监听、定时器或大图数组；地图按用户点击展开，查询缓存有边界 |
| 网络韧性 | 6/10 | 四页具备加载/错误/重试，搜索定位具备权限/精度恢复；未完成 Slow 3G/4G 真机压测 |
| 包体积 | 2/10 | Alang 158.7KiB 通过；主包实际 raw 3.268MiB、检查器选取 raw 3.215MiB、本地 Deflate 估算 1.928MiB，官方预检未完成，正式 gate 不通过 |
| **合计** | **36/60** | **BLOCK：唯一低于 4 分的维度是既有主包门禁** |

自动收集器的两个提示均已人工排除：搜索页的 `.map()` 只渲染固定 4 根信号柱，不是 50+ 条列表；`profileConstants.ts` 是 tabBar Profile 的 helper，不是新主包页面。若只评价本轮新增/修改内容的包归属（Alang 158.7KiB、无新增图片/字体），范围内分数为 42/60（WARN）；仓库正式性能结论仍保持 **BLOCK**，不以范围说明掩盖主包问题。
