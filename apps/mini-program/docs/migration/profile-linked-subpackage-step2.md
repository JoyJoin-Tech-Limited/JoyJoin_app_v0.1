# Step 2 Completion: `pages/profile-linked` Subpackage Migration

> Generated by Taro Mini-Program Frontend Engineer  
> Date: 2026-06-17  
> Scope: route-constants refactor, physical move of `edit-profile`, `rewards`, `invite`, `terms` into `pages/profile-linked`, and caller string updates.

---

## 1. Smoke-Test Fix Review

Two DevTools-discovered fixes were already on disk before this step started. I reviewed both and confirmed they are safe:

| Fix | File | What changed | Verdict |
|-----|------|--------------|---------|
| React hooks-order crash | `pages/rewards/index.tsx` | Moved early-return `<LoadingScreen>` / `<StatusCard>` blocks **after** all `useState`/`useEffect`/`useMemo`/`useCallback` hooks and derived memo computations. | ✅ Safe — fixes Rules of Hooks violation. |
| H5-only `getTabBar()` guard | `hooks/navigation/useCustomTabBarSync.ts` | Added `typeof pageWithTabBar.getTabBar !== 'function'` check before invoking `getTabBar()`. | ✅ Safe — prevents browser/H5 runtime crash. |

Validation after fixes (before any migration edits):

```bash
npm run typecheck -w mini-program   # ✅ pass
npm run guardrails                  # ✅ pass
npm run test -w mini-program        # ✅ 52 files, 355 passed, 1 skipped
```

---

## 2. Step 2 Changes Made

### 2.1 Physical file move

```
before:
  src/pages/edit-profile/*
  src/pages/rewards/*
  src/pages/invite/*
  src/pages/terms/*

after:
  src/pages/profile-linked/edit-profile/*
  src/pages/profile-linked/rewards/*
  src/pages/profile-linked/invite/*
  src/pages/profile-linked/terms/*
```

All relative imports inside the moved pages were updated:
- TSX imports: `../../lib/...`, `../../hooks/...`, `../../components/...` → `../../../lib/...`, etc.
- Inline type imports: `import('../../components/ProfessionChatOverlay')` → `import('../../../components/ProfessionChatOverlay')`
- SCSS `@use`: `../../styles/variables` → `../../../styles/variables` (same for `mixins`)

### 2.2 Route constants (`src/lib/onboarding/onboardingRoutes.ts`)

- Added `MINI_PROGRAM_PROFILE_LINKED_SUBPACKAGE_ROOT = 'pages/profile-linked'`
- Added `MINI_PROGRAM_PROFILE_LINKED_SUBPACKAGE_PAGES = ['edit-profile/index', 'rewards/index', 'invite/index', 'terms/index']`
- Added `MINI_PROGRAM_PROFILE_LINKED_PACKAGE_PAGE_PATHS` array
- Updated path constants:
  - `terms` → `pages/profile-linked/terms/index`
  - `editProfile` → `pages/profile-linked/edit-profile/index`
  - `rewards` → `pages/profile-linked/rewards/index`
  - `invite` → `pages/profile-linked/invite/index`
- Added `terms` to `MINI_PROGRAM_ROUTES`
- Removed the four pages from `MINI_PROGRAM_MAIN_PACKAGE_PAGES`
- Added the profile-linked subpackage to `MINI_PROGRAM_SUBPACKAGES`
- Added preload rule: `pages/profile/index` → `pages/profile-linked`

### 2.3 Auth allow-list (`src/lib/auth/authSessionRules.ts`)

No source change required — it consumes `MINI_PROGRAM_PAGE_PATHS.terms`, which now resolves to the new public path automatically.

### 2.4 Asset preload map (`src/lib/utils/routePreloadAssets.ts`)

- `ROUTE_PRELOAD_MAP['pages/profile-linked/rewards/index'] = REWARDS_PRELOADS`
- `PREDICTIVE_PRELOAD_MAP['pages/profile/index'] = ['pages/profile-linked/edit-profile/index']`

### 2.5 Mascot expression matrix (`src/lib/mascot/xiaoyueExpressionMatrix.ts`)

- Updated surface metadata: `pages/terms/index` → `pages/profile-linked/terms/index`

### 2.6 Hardcoded navigation strings

All hardcoded references to the old paths were replaced with `MINI_PROGRAM_ROUTES` constants:

| File | Old | New |
|------|-----|-----|
| `pages/profile/index.tsx:726` | `'/pages/terms/index'` | `MINI_PROGRAM_ROUTES.terms` |
| `pages/profile-linked/rewards/index.tsx:506` | `'/pages/invite/index'` | `MINI_PROGRAM_ROUTES.invite` |
| `pages/event-ticket-payment/index.tsx:916` | `'/pages/terms/index'` | `MINI_PROGRAM_ROUTES.terms` |
| `pages/index/LandingPage.tsx:348` | `'/pages/terms/index'` | `MINI_PROGRAM_ROUTES.terms` |
| `pages/index/LandingPage.tsx:350` | `'/pages/terms/index?section=privacy'` | `` `${MINI_PROGRAM_ROUTES.terms}?section=privacy` `` |

### 2.7 Tests

- `src/lib/onboarding/onboardingRoutes.test.ts`
  - Updated `MINI_PROGRAM_PAGES` assertion for `terms` path
  - Added profile-linked subpackage to expected `MINI_PROGRAM_SUBPACKAGES`
  - Added main-package exclusion assertions for the four moved pages
  - Added preload rule assertion for `pages/profile/index`
- `src/lib/auth/authSessionRules.test.ts` — no change needed (uses constants)

### 2.8 Documentation

- `apps/mini-program/docs/USER_FLOW.md` — updated page paths for Edit Profile, Rewards, Invite, Terms.

---

## 3. Post-Migration Validation

### 3.1 Build

```bash
cd apps/mini-program
npm run build:weapp
```

✅ Built successfully. Generated assets are now under `dist/pages/profile-linked/`:
- `dist/pages/profile-linked/edit-profile/`
- `dist/pages/profile-linked/rewards/`
- `dist/pages/profile-linked/invite/`
- `dist/pages/profile-linked/terms/`

### 3.2 Package size

```bash
npm run check:package-size
```

```
=== Package Size Breakdown ===
Core framework:        1.18 MB
Main pages:            469.6 KiB     (-75.4 KiB vs baseline)
Shared assets:         1.46 MB
─────────────────────────────────
Main package (raw):    3.10 MB       (-0.07 MB vs baseline)
Main package (zip):    1.89 MB       (-0.02 MB vs baseline)
Onboarding            505.3 KiB
Profile Linked        23.1 KiB      (new subpackage)
Icebreaker Session    29.0 KiB
Matching Status       23.2 KiB
Pool Registration     69.4 KiB
Custom tab-bar:        14.3 KiB
─────────────────────────────────
Total (zip):           2.54 MB
```

> The main-package raw size dropped by ~75 KiB and a new `Profile Linked` subpackage (23.1 KiB zip) was created. The zip delta is smaller than the raw page sizes because shared/common chunks remain in the main package, but the pages are now independently loadable.

### 3.3 Quality gates

```bash
npm run typecheck -w mini-program   # ✅ pass
npm run guardrails                  # ✅ pass
npm run test -w mini-program        # ✅ 52 files, 355 passed, 1 skipped
npm run harness:gate                # ⚠️ CONCERN (non-blocking)
```

`harness:gate` reported **CONCERN** with an overall score of 88/100. The flagged items are pre-existing and unrelated to this migration:
- Server list query pagination in `apps/server/src/routes/domains/shell.ts`
- File-size warnings for `blind-box-payment/index.tsx`, `profile/index.tsx`, `packages/shared/src/api.ts`, and a long Chinese string in `packages/shared/src/constants.ts`

No new blocking issues were introduced.

---

## 4. Remaining Work for Step 3+ (if any)

- [ ] WeChat DevTools smoke test of the migrated pages (navigation from Profile, Rewards→Invite, Event Ticket Payment→Terms, Landing legal links).
- [ ] Verify subpackage download behavior on a real device or simulator when entering `pages/profile/index`.
- [ ] Confirm public-route semantics still allow anonymous access to Terms/Privacy from the landing page.
- [ ] If the package-size saving is insufficient, consider moving additional Tier 2 assets to CDN (per the existing `check:package-size` warning).

---

## 5. Files Changed

```
apps/mini-program/src/lib/onboarding/onboardingRoutes.ts
apps/mini-program/src/lib/onboarding/onboardingRoutes.test.ts
apps/mini-program/src/lib/utils/routePreloadAssets.ts
apps/mini-program/src/lib/mascot/xiaoyueExpressionMatrix.ts
apps/mini-program/src/pages/profile/index.tsx
apps/mini-program/src/pages/event-ticket-payment/index.tsx
apps/mini-program/src/pages/index/LandingPage.tsx
apps/mini-program/src/pages/profile-linked/edit-profile/index.tsx
apps/mini-program/src/pages/profile-linked/edit-profile/index.scss
apps/mini-program/src/pages/profile-linked/rewards/index.tsx
apps/mini-program/src/pages/profile-linked/rewards/index.scss
apps/mini-program/src/pages/profile-linked/invite/index.tsx
apps/mini-program/src/pages/profile-linked/invite/index.scss
apps/mini-program/src/pages/profile-linked/terms/index.tsx
apps/mini-program/src/pages/profile-linked/terms/index.scss
apps/mini-program/docs/USER_FLOW.md
```
