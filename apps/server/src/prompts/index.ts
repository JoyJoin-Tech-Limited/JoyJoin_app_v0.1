import { XIAOYUE_PERSONA, GENDER_NEUTRAL, XIAOYUE_PERSONA_PROMPT_VERSION } from './persona';
import {
  REASONING_SCRATCHPAD,
  SMART_INFERENCE_PROMPT,
  CHAIN_OF_THOUGHT_HIDDEN,
} from './reasoning';

export {
  XIAOYUE_PERSONA,
  GENDER_NEUTRAL,
  XIAOYUE_PERSONA_PROMPT_VERSION,
};

export function getReasoningModules(): string {
  return [
    REASONING_SCRATCHPAD,
    SMART_INFERENCE_PROMPT,
    CHAIN_OF_THOUGHT_HIDDEN,
  ].join('\n\n');
}
