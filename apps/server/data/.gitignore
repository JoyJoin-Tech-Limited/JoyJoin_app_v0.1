# Auto-generated by build-occupation-vectors.mts
# Regenerate when occupations.ts changes
*.json
# occupation-vectors.json IS committed: the runtime correction-candidate top-up
# needs it in the Docker image (runner copies only dist + package.json + shared).
# Regenerate via `npx tsx scripts/build-occupation-vectors.mts` when occupations.ts
# changes, and commit the result. The offline calibration report stays ignored
# (generated locally, not shipped at runtime until a real labeled corpus exists).
!occupation-vectors.json
